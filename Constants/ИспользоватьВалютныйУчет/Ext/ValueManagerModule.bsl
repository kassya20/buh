#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

Процедура ПередЗаписью(Отказ) 
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	ТарификацияБП.КонстантаЗависимойФункциональностиПередЗаписью(
		Метаданные().Имя, Значение, "ВестиУчетПоДоговорам", Отказ);
	
КонецПроцедуры

Процедура ПриЗаписи(Отказ)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	// Функциональность Расчеты в валюте и у.е. подчинены функциональности "Учет по договорам",
	// поэтому установим эту константу тоже 
	УстановитьПривилегированныйРежим(Истина);
	Если ЭтотОбъект.Значение И Не Константы.ВестиУчетПоДоговорам.Получить() Тогда
		Константы.ВестиУчетПоДоговорам.Установить(Истина);
	КонецЕсли;
	
КонецПроцедуры

#КонецЕсли
