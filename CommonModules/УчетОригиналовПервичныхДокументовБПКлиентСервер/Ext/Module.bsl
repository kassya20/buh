Функция ИмяДекорацииСостояния() Экспорт
	
	Возврат "ДекорацияСостояниеОригиналаБП";
	
КонецФункции

Процедура УстановитьДоступностьДекорацииСостояния(Форма) Экспорт
	
	ИмяДекорацииСостояния = УчетОригиналовПервичныхДокументовБПКлиентСервер.ИмяДекорацииСостояния();
	ДекорацияСостояниеОригинала = Форма.Элементы.Найти(ИмяДекорацииСостояния);
	Если ДекорацияСостояниеОригинала = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ДекорацияСостояниеОригинала.Доступность = ЗначениеЗаполнено(Форма.Объект.Ссылка);
	
КонецПроцедуры
