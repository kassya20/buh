// @strict-types

#Область СлужебныйПрограммныйИнтерфейс

// Найти элемент формы.
// 
// Параметры:
//  Форма - ФормаКлиентскогоПриложения
//  Заголовок - Строка - заголовок элемента формы.
// 
// Возвращаемое значение:
//  Неопределено, ПолеФормы, ГруппаФормы, ДекорацияФормы, ТаблицаФормы, КнопкаФормы - 
//  																			найденный элемент или неопределено.
//
Функция НайтиЭлементФормы(Форма, Заголовок) Экспорт
	
	НайденныйЭлемент = Форма.Элементы.Найти(Заголовок);
	
	Если НайденныйЭлемент = Неопределено Тогда
		
		Для Каждого ЭлементФормы Из Форма.Элементы Цикл
			Если ЭлементФормы.Заголовок = Заголовок Тогда
				НайденныйЭлемент = ЭлементФормы;
				Прервать;
			КонецЕсли;
		КонецЦикла;
		
	КонецЕсли;
	
	Возврат НайденныйЭлемент;
	
КонецФункции

#Область СлужебныеКонстанты

// Имена элементов формы рекламы.
// 
// Возвращаемое значение:
//  Массив из Строка - Имена элементов формы рекламы
//
Функция ИмяПоляHTMLДокумента() Экспорт
	Возврат "МакетРекламныйСервис";
КонецФункции

// Имя реквизита параметры рекламного сервиса.
// 
// Возвращаемое значение:
//  Строка - Имя реквизита фонового задания
//
Функция ИмяРеквизитаПараметрРекламногоСервиса() Экспорт
	Возврат "ПараметрРекламногоСервиса";
КонецФункции

// Имя команды и элемента формы закрытия рекламы.
// 
// Возвращаемое значение:
//  Строка - имя кнопки.
//
Функция ИмяКопкиЗакрытияРекламы() Экспорт
	Возврат "ЗакрытьРекламу";
КонецФункции

// Имя реквизита обратного отсчета кнопки закрытия рекламного носителя на форме.
// 
// Возвращаемое значение:
//  Строка - Имя реквизита обратного отсчета кнопки закрытия
Функция ИмяРеквизитаОбратногоОтсчетаКнопкиЗакрытия() Экспорт
	Возврат "ОбратныйОтсчетКнопкиЗакрытияРекламы";
КонецФункции

// Имя группы рекламный сервис.
// 
// Возвращаемое значение:
//  Строка - Имя группы рекламный сервис
//
Функция ИмяГруппыРекламныйСервис() Экспорт
	Возврат "ГруппаРекламныйСервис";
КонецФункции

#КонецОбласти

#КонецОбласти
