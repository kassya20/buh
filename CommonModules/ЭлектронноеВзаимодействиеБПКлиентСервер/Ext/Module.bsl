////////////////////////////////////////////////////////////////////////////////
// ЭлектронноеВзаимодействиеБПКлиент: вспомогательные процедуры и функции БЭД
//
////////////////////////////////////////////////////////////////////////////////

#Область СлужебныйПрограммныйИнтерфейс

#Область ЗагрузкаЭД

Функция ВариантыПолученияФайлов() Экспорт
	
	ВариантыПолученияФайлов = Новый Структура();
	ВариантыПолученияФайлов.Вставить("СКомпьютера", "СКомпьютера");
	ВариантыПолученияФайлов.Вставить("ИзПочты", "ИзПочты");
	Возврат ВариантыПолученияФайлов;
	
КонецФункции

Функция РасширениеПоддерживается(РасширениеБезТочки, ВариантЗагрузкиЭД = Неопределено) Экспорт
	
	Если ВариантЗагрузкиЭД = ПредопределенноеЗначение("Перечисление.ВариантыЗагрузкиЭД.СчетНаОплату") Тогда
		ДоступныеФорматы = "zip;xml";
	ИначеЕсли ВариантЗагрузкиЭД = ПредопределенноеЗначение("Перечисление.ВариантыЗагрузкиЭД.РеализацияТоваровИУслуг") Тогда
		ДоступныеФорматы = "mxl;xls;xlsx;zip;xml";
	Иначе
		ДоступныеФорматы = "zip;xml";
	КонецЕсли;
	
	Возврат СтрНайти(ДоступныеФорматы, нрег(РасширениеБезТочки)) > 0;
КонецФункции

#КонецОбласти

#КонецОбласти




