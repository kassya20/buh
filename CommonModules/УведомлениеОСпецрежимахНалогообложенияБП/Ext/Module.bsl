
#Область ПодпискаНаСобытие

Процедура УведомленияОСпецрежимахНалогообложенияПередЗаписью(Источник, Отказ, РежимЗаписи, РежимПроведения) Экспорт
	
	Если Источник.ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	Если Отказ Тогда
		Возврат;
	КонецЕсли;
		
	Если Источник.ВидУведомления = Перечисления.ВидыУведомленийОСпецрежимахНалогообложения.ИзменениеПорядкаИсчисленияАвансовПоНалогуНаПрибыль
		И Не Источник.ПометкаУдаления И Не ЗначениеЗаполнено(Источник.Ссылка) Тогда
		
		// Статистику пишем при первоначальной записи уведомления.
		ЦентрМониторинга.ЗаписатьОперациюБизнесСтатистики("СтатистикаБП.УведомлениеОбИзмененииПорядкаУплатыАвансовПоНалогуНаПрибыль2022", 1);
	КонецЕсли;
	
КонецПроцедуры

Процедура УведомленияОСпецрежимахНалогообложенияПриЗаписи(Источник, Отказ, РежимЗаписи, РежимПроведения) Экспорт
	
	Если Источник.ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	Если Отказ Тогда
		Возврат;
	КонецЕсли;
		
	Если Источник.ВидУведомления = Перечисления.ВидыУведомленийОСпецрежимахНалогообложения.ИсчисленныеСуммыНалогов Тогда
		ЕдиныйНалоговыйСчет.СинхронизироватьСостояниеУведомлений(Источник);
	КонецЕсли;
	
	Если Источник.ВидУведомления =
		Перечисления.ВидыУведомленийОСпецрежимахНалогообложения.УменьшениеНалогаНаСтраховыеВзносы Тогда
		УчетПСН.СинхронизироватьСостояниеУведомлений(Источник);
	КонецЕсли;
	
КонецПроцедуры
	
#КонецОбласти