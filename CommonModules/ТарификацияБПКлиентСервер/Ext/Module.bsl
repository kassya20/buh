#Область ПрограммныйИнтерфейс

// Возвращает знак рубля (₽).
//
//	Возвращаемое значение: Строка - Знак рубля (₽)
Функция ЗнакРубля() Экспорт
	
	Возврат Символ(8381);
	
КонецФункции

// Обработчик подписки на событие ПроверкаДоступностиФормыПоТарифу, см. ТарификацияБП.УстановитьДоступностьФормы
//
Процедура ПроверкаДоступностиФормыПоТарифуОбработкаПолученияФормы(Источник, ВидФормы, Параметры, ВыбраннаяФорма, ДополнительнаяИнформация, СтандартнаяОбработка) Экспорт
	
	#Если Сервер Тогда
		ТарификацияБП.ПроверкаДоступностиФормыПоТарифуОбработкаПолученияФормы(
			Источник, ВидФормы, Параметры, ВыбраннаяФорма, ДополнительнаяИнформация, СтандартнаяОбработка);
	#КонецЕсли
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс

Функция ПолучитьТекстПредупреждения(Заголовок = Неопределено) Экспорт
	
	Если Заголовок = Неопределено Тогда
		Заголовок = НСтр("ru='Эта возможность доступна на платном тарифе.'");
	КонецЕсли;
	
	ТекстПредупреждения = Новый ФорматированнаяСтрока(
		Новый ФорматированнаяСтрока(Заголовок, Новый Шрифт(, 12, Истина)),
		Символы.ПС,
		Новый ФорматированнаяСтрока(НСтр("ru='Перейдите на платный тариф'"),,,, ИмяСсылкиДляПерехода()),
		НСтр("ru=', чтобы пользоваться всеми возможностями без ограничений.'"),
		Символы.ПС);
	
	Возврат ТекстПредупреждения;
	
КонецФункции

Функция ГруппаСобытийЖурналаРегистрации() Экспорт
	
	КодЯзыка = ОбщегоНазначенияКлиентСервер.КодОсновногоЯзыка();
	Возврат НСтр("ru='Ограничение тарифа'", КодЯзыка);
	
КонецФункции

Функция ИмяДействияОплатаСервиса() Экспорт
	
	Возврат "ОплатаСервиса";
	
КонецФункции

Функция ИмяДействияНастройкиОрганизации() Экспорт
	
	Возврат "НастройкиОрганизации";
	
КонецФункции

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Функция ИмяСсылкиДляПерехода()
	
	ИмяСсылки = "";
	
	Если ТарификацияБПВызовСервера.ДоступнаОплатаСервиса() Тогда
		
		ИмяСсылки = "e1cib/command/Обработка.ОплатаСервисаБП.Команда.ИзменитьТариф";
		
	КонецЕсли;
	
	Возврат ИмяСсылки;
	
КонецФункции

#КонецОбласти
