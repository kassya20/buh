
////////////////////////////////////////////////////////////////////////////////
// Подсистема "Облачные классификаторы".
// ОбщийМодуль.ОблачныеКлассификаторыПереопределяемый.
////////////////////////////////////////////////////////////////////////////////

#Область ПрограммныйИнтерфейс

#Область КлассификаторТНВЭД

// Определение загруженных элементов ТНВЭД
// Процедура используется в форме онлайн-подбора элементов классификатора ТН ВЭД
// для установки отметок у имеющихся в базе элементов (в режиме загрузки элементов),
// а также при обновлении классификатора и в других служебных методах.
//
// Параметры:
//  Элементы - Массив - коды имеющихся в базе элементов классификатора. Элементы массива должны иметь тип Строка.
//                      Допустимо передавать коды в формате с пробелами так, как они указываются в классификаторе
//                      ("ХХХХ ХХ ХХХ Х" - 13 символов) или без ("ХХХХХХХХХХ" - 10 символов).
//
Процедура ОпределитьЗагруженныеЭлементыТНВЭД(Элементы) Экспорт
	
	Справочники.КлассификаторТНВЭД.ОпределитьЗагруженныеЭлементыТНВЭДИзОблачногоКлассификатора(Элементы);
	
КонецПроцедуры

// Создание или обновление элементов ТНВЭД
//
// Параметры:
//  ДанныеСервиса - ТаблицаЗначений - данные, полученные из сервиса. Колонки:
//                    * Идентификатор           - Число  - идентификатор элемента на стороне сервиса (служебное);
//                    * Код                     - Строка - код элемента классификатора в формате "ХХХХ ХХ ХХХ Х";
//                    * КодРодителя             - Строка - код элемента, являющегося родителем, в формате с пробелами;
//                    * Порядок                 - Число  - поле для упорядочивания элементов (служебное);
//                    * ДатаНачалаДействия      - Дата   - дата начала действия элемента классификатора;
//                    * ДатаОкончанияДействия   - Дата   - дата окончания действия элемента классификатора;
//                    * Наименование            - Строка - наименование элемента;
//                    * НаименованиеПолное      - Строка - наименование элемента, включающее наименования родителей;
//                    * Описание                - Строка - описание элемента;
//                    * КодОКЕИ                 - Строка - код элемента справочника ОКЕИ;
//                    * Сырьевой                - Булево - признак, указывающий на принадлежность товаров к сырьевым;
//                    * ТаможеннаяПошлина       - Строка - ставка таможенной пошлины и дополнительная информация;
//                    * СтавкаНДС               - Строка - ставка НДС;
//                    * ПодлежитУтилизации      - Булево - признак, указывающий на необходимость утилизации товаров;
//                    * ИзменениеСоставаТоваров - Булево - признак, указывающий на изменение состава товаров,
//                                                         относящихся к элементу;
//  Отказ - Булево - если Истина, обновление будет считаться неуспешным.
//
Процедура СоздатьОбновитьЭлементыТНВЭД(ДанныеСервиса, Отказ) Экспорт
	
	Справочники.КлассификаторТНВЭД.СоздатьОбновитьЭлементыТНВЭДИзОблачногоКлассификатора(ДанныеСервиса, Отказ);
	
КонецПроцедуры

#КонецОбласти

#Область КлассификаторОКПД2

// Определение загруженных элементов ОКПД 2
// Процедура заполняет переданный параметр массивом кодов классификатора, имеющихся в базе.
// Используется в форме онлайн-подбора элементов классификатора 
// для установки отметок у имеющихся в базе элементов (в режиме загрузки элементов),
// а также при обновлении классификатора и в других служебных методах.
//
// Параметры:
//  ЭлементыКлассификатора - Массив - коды имеющихся в базе элементов классификатора. 
//        Элементы массива должны иметь тип Строка(12).
//          Форматы кодов:
//          А - Раздел;
//          ХХ - Класс;
//          ХХ.Х - Подкласс;
//          ХХ.ХХ - Группа;
//          ХХ.ХХ.Х - Подгруппа;
//          ХХ.ХХ.ХХ - Вид;
//          ХХ.ХХ.ХХ.ХХ0 - Категория;
//          ХХ.ХХ.ХХ.ХХХ - Подкатегория;
//          где Х - цифры от 0 до 9; А - буква от А до Я
//
Процедура ОпределитьЗагруженныеЭлементыОКПД2(ЭлементыКлассификатора) Экспорт
	
	Справочники.КлассификаторОКПД2.ОпределитьЗагруженныеЭлементыОКПД2ИзОблачногоКлассификатора(ЭлементыКлассификатора);
	
КонецПроцедуры

// Создание или обновление элементов классификатора ОКПД 2
//
// Параметры:
//  ДанныеСервиса - ТаблицаЗначений - данные, полученные из сервиса. Колонки:
//    * Идентификатор           - Число - идентификатор элемента на стороне сервиса (служебное);
//    * Код                     - Строка - код элемента классификатора;
//    * КодРодителя             - Строка - код родителя элементов;
//    * Порядок                 - Число - поле для упорядочивания элементов (служебное);
//    * ДатаНачалаДействия      - Дата - дата начала действия элемента классификатора;
//    * ДатаОкончанияДействия   - Дата - дата окончания действия элемента классификатора;
//    * Наименование            - Строка - наименование элемента;
//    * Описание                - Строка - описание элемента;
//    * КодОКЕИ                 - Строка - код элемента справочника ОКЕИ;
//  Отказ - Булево - если Истина, обновление будет считаться не успешным.
//
Процедура СоздатьОбновитьЭлементыОКПД2(ДанныеСервиса, Отказ) Экспорт
	
	Справочники.КлассификаторОКПД2.СоздатьОбновитьЭлементыОКПД2ИзОблачногоКлассификатора(ДанныеСервиса, Отказ);
	
КонецПроцедуры

#КонецОбласти

#Область КлассификаторОКЕИ

// Определяет загруженные элементы ОКЕИ.
//
// Параметры:
//   СсылкиПоКоду            - Соответствие из КлючИЗначение - загруженные единицы измерения:
//     * Ключ                  - Строка - код единицы измерения.
//     * Значение              - ОпределяемыйТип.УпаковкаНоменклатурыБЭД - ссылка на единицу измерения.
//   ДополнительныеПараметры - Неопределено, Структура из КлючИЗначение - дополнительные параметры.
//
Процедура ОпределитьЗагруженныеЭлементыОКЕИ(СсылкиПоКоду, ДополнительныеПараметры = Неопределено) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	ЕдиницыИзмерения.Код КАК Код,
		|	ЕдиницыИзмерения.Ссылка КАК Ссылка
		|ИЗ
		|	Справочник.КлассификаторЕдиницИзмерения КАК ЕдиницыИзмерения
		|ГДЕ
		|	ЕдиницыИзмерения.Код <> """"";
	
	УстановитьПривилегированныйРежим(Истина);
	ВыборкаДанных = Запрос.Выполнить().Выбрать();
	УстановитьПривилегированныйРежим(Ложь);
	
	СсылкиПоКоду.Очистить();
	Пока ВыборкаДанных.Следующий() Цикл
		СсылкиПоКоду.Вставить(СокрЛП(ВыборкаДанных.Код), ВыборкаДанных.Ссылка);
	КонецЦикла;
	
КонецПроцедуры

// Проверяет необходимость добавления элемента ОКЕИ, например, по установленному отбору.
//
// Параметры:
//   Строка - СтрокаТаблицыЗначений - элемент ОКЕИ для проверки:
//     * Код                                      - Строка - например, "163".
//     * Наименование                             - Строка - например, "Грамм".
//     * УсловноеОбозначениеНациональное          - Строка - например, "г".
//     * УсловноеОбозначениеМеждународное         - Строка - например, "g".
//     * КодовоеБуквенноеОбозначениеНациональное  - Строка - например, "Г".
//     * КодовоеБуквенноеОбозначениеМеждународное - Строка - например, "GRM".
//     * ТипИзмеряемойВеличины                    - Строка - например, "Вес".
//     * Числитель                                - Число  - числитель в отношении к базовой единице измерения.
//                                                    Например, для грамма базовой единицей измерения является
//                                                    килограмм, т.е. числитель равен 1.
//     * Знаменатель                              - Число  - знаменатель в отношении к базовой единице измерения.
//                                                    Например, для грамма знаменатель равен 1000.
//     * Пометка                                  - Булево - пометка элемента для подбора.
//     * Ссылка                                   - ОпределяемыйТип.УпаковкаНоменклатурыБЭД - ссылка на существующий 
//                                                    элемент справочника.
//   ДополнительныеПараметры - Неопределено, Структура из КлючИЗначение - дополнительные параметры.
//   Отказ                   - Булево - признак отказа от добавления элемента.
//
Процедура ПроверитьЭлементОКЕИ(Строка, ДополнительныеПараметры = Неопределено, Отказ = Ложь) Экспорт
	
КонецПроцедуры

// Создает или обновляет единицу измерения по данным ОКЕИ.
//
// Параметры:
//   ДанныеСервиса - СтрокаДереваЗначений - элемент классификатора:
//     * Код                                      - Строка - например, "163".
//     * Наименование                             - Строка - например, "Грамм".
//     * УсловноеОбозначениеНациональное          - Строка - например, "г".
//     * УсловноеОбозначениеМеждународное         - Строка - например, "g".
//     * КодовоеБуквенноеОбозначениеНациональное  - Строка - например, "Г".
//     * КодовоеБуквенноеОбозначениеМеждународное - Строка - например, "GRM".
//     * ТипИзмеряемойВеличины                    - Строка - например, "Вес".
//     * Числитель                                - Число  - числитель в отношении к базовой единице измерения.
//                                                    Например, для грамма базовой единицей измерения является
//                                                    килограмм, т.е. числитель равен 1.
//     * Знаменатель                              - Число  - знаменатель в отношении к базовой единице измерения.
//                                                    Например, для грамма знаменатель равен 1000.
//     * Пометка                                  - Булево - пометка элемента для подбора.
//     * Ссылка                                   - ОпределяемыйТип.УпаковкаНоменклатурыБЭД - ссылка на существующий 
//                                                    элемент справочника.
//   Ссылка   - Неопределено, ОпределяемыйТип.УпаковкаНоменклатурыБЭД - ссылка на созданную (измененную) единицу измерения.
//   ЭтоНовый - Булево - признак созданной единицы измерения.
//
Процедура СоздатьОбновитьЭлементОКЕИ(ДанныеСервиса, Ссылка = Неопределено, ЭтоНовый = Ложь) Экспорт
	
	Если ЗначениеЗаполнено(ДанныеСервиса.Ссылка) Тогда
		СправочникОбъект = ДанныеСервиса.Ссылка.ПолучитьОбъект();
	Иначе
		ЭтоНовый = Истина;
		СправочникОбъект = Справочники.КлассификаторЕдиницИзмерения.СоздатьЭлемент();
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ДанныеСервиса.УсловноеОбозначениеНациональное) Тогда
		Наименование = ДанныеСервиса.УсловноеОбозначениеНациональное;
	ИначеЕсли ЗначениеЗаполнено(ДанныеСервиса.УсловноеОбозначениеМеждународное) Тогда
		Наименование = ДанныеСервиса.УсловноеОбозначениеМеждународное;
	ИначеЕсли ЗначениеЗаполнено(ДанныеСервиса.КодовоеБуквенноеОбозначениеНациональное) Тогда
		Наименование = ДанныеСервиса.КодовоеБуквенноеОбозначениеНациональное;
	ИначеЕсли ЗначениеЗаполнено(ДанныеСервиса.КодовоеБуквенноеОбозначениеМеждународное) Тогда
		Наименование = ДанныеСервиса.КодовоеБуквенноеОбозначениеМеждународное;
	Иначе
		Наименование = ДанныеСервиса.Наименование;
	КонецЕсли;
	
	СправочникОбъект.Код                     = СокрЛП(ДанныеСервиса.Код);
	СправочникОбъект.Наименование            = СтрЗаменить(Наименование,Символы.ПС, "/");
	СправочникОбъект.НаименованиеПолное      = СтрЗаменить(ДанныеСервиса.Наименование,Символы.ПС, "/");
	
	СправочникОбъект.Записать();
	
	Ссылка = СправочникОбъект.Ссылка;
	
КонецПроцедуры

#КонецОбласти

#КонецОбласти
