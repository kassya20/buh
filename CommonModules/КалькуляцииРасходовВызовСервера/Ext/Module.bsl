#Область СлужебныйПрограммныйИнтерфейс

Функция АдресВременногоХранилищаСРасчетамиКалькуляции(Форма, КлючКалькуляцииРасходов) Экспорт
	
	Если Не ЗначениеЗаполнено(КлючКалькуляцииРасходов) Тогда
		Возврат ПоместитьВоВременноеХранилище(Неопределено, Форма.УникальныйИдентификатор);
	КонецЕсли;
	
	Отбор = Новый Структура;
	Отбор.Вставить("КлючКалькуляцииРасходов", КлючКалькуляцииРасходов);
	СтрокиКалькуляцииРасходов = Форма.КалькуляцияРасходов.НайтиСтроки(Отбор);
	
	ТаблицаРасходов = Форма.КалькуляцияРасходов.Выгрузить(СтрокиКалькуляцииРасходов);
	Возврат ПоместитьВоВременноеХранилище(ТаблицаРасходов, Форма.УникальныйИдентификатор);
	
КонецФункции

#КонецОбласти