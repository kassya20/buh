#Область ПрограммныйИнтерфейс

Функция СверитьДанныеДляПомощникуПоУчетуНДС(СтруктураПараметров) Экспорт
	
	ПараметрыВыполнения = ДлительныеОперации.ПараметрыВыполненияВФоне(СтруктураПараметров.УникальныйИдентификатор);
	ПараметрыВыполнения.НаименованиеФоновогоЗадания = НСтр("ru = 'Сверка данных для Помощника учета НДС'");
	ПараметрыВыполнения.ЗапуститьВФоне = Истина;
	Возврат ДлительныеОперации.ВыполнитьПроцедуру(
		ПараметрыВыполнения,
		"СервисСверкиРасчетовБП.СверитьКнигиДляПомощникаПоНДСВФоне",
		СтруктураПараметров);
	
КонецФункции

#КонецОбласти
