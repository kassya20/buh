///////////////////////////////////////////////////////////////////////////////////////////////////////
// Copyright (c) 2022, ООО 1С-Софт
// Все права защищены. Эта программа и сопроводительные материалы предоставляются
// в соответствии с условиями лицензии Attribution 4.0 International (CC BY 4.0)
// Текст лицензии доступен по ссылке:
// https://creativecommons.org/licenses/by/4.0/legalcode
///////////////////////////////////////////////////////////////////////////////////////////////////////

//////////////////////////////////////////////////////////////////////////////////
// Подсистема "Настройки программы".
// ОбщийМодуль.НастройкиПрограммыБИППереопределяемый.
//
////////////////////////////////////////////////////////////////////////////////

#Область ПрограммныйИнтерфейс

// Предназначена для внесения изменений в форму ИнтернетПоддержкаИСервисы обработки ПанельАдминистрированияБИП без
// снятия формы с поддержки.
//
// Параметры:
//  Форма - см. Обработка.ПанельАдминистрированияБИП.Форма.ИнтернетПоддержкаИСервисы
//
Процедура ПриСозданииФормыИнтернетПоддержкаИСервисы(Форма) Экспорт
	
	Форма.Элементы.ГруппаОткрытьОписаниеИзмененийСистемы.Видимость = Ложь;
	
	// Переопределение элементов для автозагрузки патчей.
	Если ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(Форма.Элементы, "ГруппаОбновлениеПрограммыИсправления")  Тогда
		Форма.Элементы.ГруппаОбновлениеПрограммыИсправления.ЦветФона = ЦветаСтиля.БыстрыеОтборыФонГруппы;
	КонецЕсли;
	
	Если ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(Форма.Элементы, "БИПЗагружатьИУстанавливатьИсправленияАвтоматически")  Тогда
		ЭлементАвтозагрузкаПатчей = Форма.Элементы.БИПЗагружатьИУстанавливатьИсправленияАвтоматически;
		ЭлементАвтозагрузкаПатчей.Заголовок = НСтр("ru = 'Загружать и устанавливать исправления (быстрые обновления) автоматически'");
		ЭлементАвтозагрузкаПатчей.ШрифтЗаголовка = Новый Шрифт(,, Истина); // полужирный
		ЭлементАвтозагрузкаПатчей.ВысотаЗаголовка = 2;
		Если Форма.Параметры.Свойство("ФокусНаФлагеАвтозагрузкаПатчей")
				И Форма.Параметры.ФокусНаФлагеАвтозагрузкаПатчей Тогда
			Форма.ТекущийЭлемент = ЭлементАвтозагрузкаПатчей;
		КонецЕсли;
	КонецЕсли;
	
	ИнтеграцияСтатусСамозанятого.ПриСозданииФормыИнтернетПоддержкаИСервисы(Форма);
	
КонецПроцедуры

#КонецОбласти