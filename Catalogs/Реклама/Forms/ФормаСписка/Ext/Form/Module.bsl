// @strict-types

#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	ТолькоПросмотр = Истина;
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура ИзображениеПриНажатии(Элемент, ДанныеСобытия, СтандартнаяОбработка)
	РекламныйСервисКлиент.МакетРекламныйСервисНажатие(ЭтотОбъект, ДанныеСобытия, СтандартнаяОбработка);
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовТаблицыФормыСписок

&НаКлиенте
Процедура СписокПриАктивизацииСтроки(Элемент)
	
	ПолеИзображения = "";
	ТекущиеДанные = Элементы.Список.ТекущиеДанные;
	Если ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
	РекламаСсылка = ТекущиеДанные.Ссылка; // СправочникСсылка.Реклама
	СписокПриАктивизацииСтрокиНаСервере(РекламаСсылка);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Список при активизации строки на сервере.
// 
// Параметры:
//  Реклама - СправочникСсылка.Реклама
//
&НаСервере
Процедура СписокПриАктивизацииСтрокиНаСервере(Реклама)
	
	РекламныйСервисСлужебный.СформироватьИзображениеРекламыНаФорме(Реклама, ПолеИзображения);
	
КонецПроцедуры

#КонецОбласти