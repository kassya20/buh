#Область ОписаниеПеременных

// Хранит Истина, если запись ссылки на файл договора произведено 
// в основной форме элемента справочника, открытом в другом окне.
&НаКлиенте
Перем СозданиеФайлаОбработаноОсновнойФормойДоговора;

// Хранит выбранные форматы сохранения файла договора
&НаКлиенте
Перем ФорматыСохранения;

&НаКлиенте
Перем ПараметрыОтправкиПисьма;

#КонецОбласти

#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("АвтоТест") Тогда
		Возврат;
	КонецЕсли;
	
	ЭлектронныйФормат = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(Параметры, "ЭлектронныйФормат", Ложь);
		
	ИспользоватьФайлДоговора = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(Параметры, "ИспользоватьФайлДоговора", Истина);

	ЦветФонаЗаменяемогоПараметра = ПечатьДоговоров.ЦветФонаЗаменяемогоПараметра();

	// Если задан файл договора, открываем именно его,
	// иначе получаем договор из объекта для печати.
	Если ЗначениеЗаполнено(Параметры.ФайлДоговора) И ИспользоватьФайлДоговора Тогда
		
		ФайлДоговора = Параметры.ФайлДоговора;
		
		ДоговорКонтрагента = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ФайлДоговора, "ВладелецФайла");
		
		// Если не передано при открытии, получим тип из договора 
		Если Не ЭлектронныйФормат Тогда
			// Если не передано при открытии, получим тип из договора
			ЭлектронныйФормат = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ДоговорКонтрагента, "ЭлектронныйФормат");
		КонецЕсли;

		Если ЗначениеЗаполнено(Параметры.ОбъектПечати) Тогда
			ОбъектПечати = Параметры.ОбъектПечати;
		Иначе
			ОбъектПечати = ДоговорКонтрагента;
		КонецЕсли;
		
	ИначеЕсли ЗначениеЗаполнено(Параметры.ШаблонДоговора) Тогда
	
		ШаблонДоговора = Параметры.ШаблонДоговора;
		ОбъектПечати = Параметры.ОбъектПечати;
		
	Иначе
		
		ОбъектПечати = Параметры.ОбъектПечати;
		
	КонецЕсли;

	Если НЕ ЗначениеЗаполнено(ОбъектПечати) Тогда
		ВызватьИсключение НСтр("ru = 'Не определен договор или документ, для которого необходимо распечатать текст договора.'");
	КонецЕсли;

	ЗагрузитьСведенияОДоговореКонтрагента();
	
	Если ЗначениеЗаполнено(Параметры.РеквизитыДополнительногоСоглашения) Тогда
		ЭтоДополнительноеСоглашение = Истина;
		ЗаполнитьЗначенияСвойств(ЭтотОбъект, Параметры.РеквизитыДополнительногоСоглашения);
		ДоговорПодписан = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(
			Параметры.РеквизитыДополнительногоСоглашения, "СоглашениеПодписано", Ложь);
		ФайлДоговора = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(
			Параметры.РеквизитыДополнительногоСоглашения, "ФайлСоглашения", Неопределено);
		ДоговорПодписан = ДоговорПодписан И ЗначениеЗаполнено(ФайлДоговора);
		ЭлектронныйФормат = Ложь;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ФайлДоговора) Тогда
		
		ПроверитьФайлАктуаленИУстановитьТолькоПросмотр(Отказ);
		
		// Если Файл договора не актуален,
		// тогда ПриОткрытии получим новую версию файла
		Если ТекущийФайлАктуален Тогда

			ЗагрузитьФайлДоговора();
			
		КонецЕсли;
		
	Иначе
		
		Если ЗначениеЗаполнено(ШаблонДоговора) Тогда
			
			ТекстПросьбы = Символы.ПС + Символы.Таб + НСтр("ru = 'Пожалуйста, подождите...'");
			ШрифтПросьбы = Новый Шрифт( , 18, Истина);
			ПросьбаПодождать = Новый ФорматированнаяСтрока(ТекстПросьбы, ШрифтПросьбы);
			ФорматированныйДокумент.УстановитьФорматированнуюСтроку(ПросьбаПодождать);
			
			ЗагрузитьШаблонДоговора();
			
		Иначе
			// Нигде не смогли найти, какой шаблон использовать.
			ВызватьИсключение НСтр("ru = 'Не указан шаблон договора для редактирования текста договора.'");
		КонецЕсли;
		
	КонецЕсли;
	
	УправлениеФормой();
	
	Если НЕ ТолькоПросмотр Тогда
		ДобавитьДополнительныеРеквизитыИДопСвойства()
	КонецЕсли;
	
	СоздатьОбъектДоговорКонтрагента();
	
	// ЭлектронноеВзаимодействие.ОбменСКонтрагентами
	Если ТипЗнч(ОбъектПечати) = Тип("СправочникСсылка.ДоговорыКонтрагентов")
		 И ЭлектронныйФормат Тогда
		ПараметрыПриСозданииНаСервере = ОбменСКонтрагентами.ПараметрыПриСозданииНаСервере_ФормаСправочника();
		ПараметрыПриСозданииНаСервере.Форма                  = ЭтотОбъект;
		ПараметрыПриСозданииНаСервере.СправочникСсылка       = ДоговорКонтрагента;
		ПараметрыПриСозданииНаСервере.МестоРазмещенияКоманд  = Элементы.КомандыЭДО;
		ПараметрыПриСозданииНаСервере.КонтроллерСостояниеЭДО = Элементы.ДекорацияСостояниеЭДО;
		ПараметрыПриСозданииНаСервере.ГруппаСостояниеЭДО     = Элементы.ГруппаСостояниеЭДО;
		ОбменСКонтрагентами.ПриСозданииНаСервере_ФормаСправочника(ПараметрыПриСозданииНаСервере);
	КонецЕсли;
	// Конец ЭлектронноеВзаимодействие.ОбменСКонтрагентами
	
	Если ОбщегоНазначения.ЭтоМобильныйКлиент() Тогда
		ДоступныУчетныеЗаписиДляОтправкиМК = Взаимодействия.ИспользуетсяПочтовыйКлиент()
			Или РаботаСПочтовымиСообщениямиВызовСервера.ЕстьДоступныеУчетныеЗаписиДляОтправки();
	КонецЕсли;
	
	МобильныйКлиентАдаптацияФормы();
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	Если ЗначениеЗаполнено(ФайлДоговора) Тогда
		Если Не ТекущийФайлАктуален Тогда
			ПодключитьОбработчикОжидания("Подключаемый_ОбработчикОжиданияПолучитьИЗагрузитьФайлИзХранилища", 0.1, Истина);
		КонецЕсли;
	Иначе
		Если ДлительнаяОперация <> Неопределено Тогда
			ПодключитьОбработчикОжидания("Подключаемый_ОжидатьЗагрузкиШаблона", 0.1, Истина);
		КонецЕсли;
	КонецЕсли;
	
	// ЭлектронноеВзаимодействие.ОбменСКонтрагентами    
	Если ТипЗнч(ОбъектПечати) = Тип("СправочникСсылка.ДоговорыКонтрагентов") 
		 И ЭлектронныйФормат Тогда
		ПараметрыПриОткрытии = ОбменСКонтрагентамиКлиент.ПараметрыПриОткрытии();
		ПараметрыПриОткрытии.Форма = ЭтотОбъект;
		ПараметрыПриОткрытии.ЕстьОбработчикОбновитьКомандыЭДО = Истина;
		
		ОбменСКонтрагентамиКлиент.ПриОткрытии(ПараметрыПриОткрытии);  
	КонецЕсли;
	// Конец ЭлектронноеВзаимодействие.ОбменСКонтрагентами
	
КонецПроцедуры

&НаКлиенте
Процедура ПередЗакрытием(Отказ, ЗавершениеРаботы, ТекстПредупреждения, СтандартнаяОбработка)
	
	Если ЗавершениеРаботы И Модифицированность Тогда
		Отказ = Истина;
		Возврат;
	КонецЕсли;
		
	Если Модифицированность Тогда
	
		Если Элементы.ФорматированныйДокумент.ТолькоПросмотр Тогда
		
			СтандартнаяОбработка = Ложь;
		
		Иначе
		
			// На форме нет основного реквизита, поэтому спрашивает о необходимости 
			// сохранения изменений самостоятельно.
			ОписаниеОповещения = Новый ОписаниеОповещения("ПередЗакрытиемЗавершение", ЭтотОбъект);
			
			ПоказатьВопрос(ОписаниеОповещения, 
				НСтр("ru = 'Данные были изменены. Сохранить изменения?'"),
				РежимДиалогаВопрос.ДаНетОтмена,
				,
				КодВозвратаДиалога.Да);
			
			Отказ = Истина;
		
		КонецЕсли;
	
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ОбработкаОповещения(ИмяСобытия, Параметр, Источник)
	
	Если ИмяСобытия = "ДоговорыКонтрагентов_СозданиеФайлаОбработаноОсновнойФормойДоговора"
		И Параметр = ДоговорКонтрагента Тогда
		// Запомним, что ссылка на файл записана в основной форме элемента справочника,
		// и дополнительно ее записывать не требуется.
		СозданиеФайлаОбработаноОсновнойФормойДоговора = Истина;
	
	КонецЕсли;
	
	// ЭлектронноеВзаимодействие.ОбменСКонтрагентами     
	Если ТипЗнч(ОбъектПечати) = Тип("СправочникСсылка.ДоговорыКонтрагентов") 
		 И ЭлектронныйФормат Тогда
		ПараметрыОповещенияЭДО                        = ОбменСКонтрагентамиКлиент.ПараметрыОповещенияЭДО_ФормаСправочника();
		ПараметрыОповещенияЭДО.Форма                  = ЭтотОбъект;
		ПараметрыОповещенияЭДО.СправочникСсылка       = ДоговорКонтрагента;
		ПараметрыОповещенияЭДО.КонтроллерСостояниеЭДО = Элементы.ДекорацияСостояниеЭДО;
		ПараметрыОповещенияЭДО.ГруппаСостояниеЭДО     = Элементы.ГруппаСостояниеЭДО;
		
		ОбменСКонтрагентамиКлиент.ОбработкаОповещения_ФормаСправочника(ИмяСобытия, Параметр, Источник,
			ПараметрыОповещенияЭДО);
	КонецЕсли;
	// Конец ЭлектронноеВзаимодействие.ОбменСКонтрагентами
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработкаВыбора(ВыбранноеЗначение, ИсточникВыбора)
	
	Если ВРег(ИсточникВыбора.ИмяФормы) = ВРег("Справочник.ДоговорыКонтрагентов.Форма.СохранениеПечатнойФормы") Тогда
		
		Если ВыбранноеЗначение <> Неопределено И ВыбранноеЗначение <> КодВозвратаДиалога.Отмена Тогда
			
			ФорматыСохранения = ВыбранноеЗначение.ФорматыСохранения;
			Если ВыбранноеЗначение.ВариантСохранения = "СохранитьВПапку" Тогда
				СохранитьДоговорВПапку(ВыбранноеЗначение.ПапкаДляСохранения);
			Иначе
				ЗаписатьФайлДоговораНаКлиенте();
				Состояние(НСтр("ru = 'Сохранение успешно завершено.'"));
			КонецЕсли;
		КонецЕсли;
		
	ИначеЕсли ВРег(ИсточникВыбора.ИмяФормы) = ВРег("Справочник.ДоговорыКонтрагентов.Форма.ОтправкаДополнительногоСоглашенияПоЭДО") Тогда
		
		Если ВыбранноеЗначение <> Неопределено И ВыбранноеЗначение <> КодВозвратаДиалога.Отмена Тогда
			ДанныеФайла = ПолучитьФайлДополнительногоСоглашенияДляЭДО(ВыбранноеЗначение);
			ОтправитьФайлСоглашенияПоЭДО(ДанныеФайла);
		КонецЕсли;
		
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура КартинкаЗакрытьБаннерНажатие(Элемент)
	ЗакрытьБаннерПодсказка();
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийКомандныхПанелейФормы

&НаКлиенте
Процедура Сохранить(Команда)
	
	ПараметрыФормы = Новый Структура;
	ПараметрыФормы.Вставить("ОбъектПечати", ДоговорКонтрагента);
	
	ОткрытьФорму("Справочник.ДоговорыКонтрагентов.Форма.СохранениеПечатнойФормы", ПараметрыФормы, ЭтотОбъект);
	
КонецПроцедуры

&НаКлиенте
Процедура Отправить(Команда)

	ПараметрыФормы = Новый Структура();
	ПараметрыФормы.Вставить("ОбъектОтправки", ОбъектПечати);

	ОписаниеОповещения = Новый ОписаниеОповещения("ОтправитьЗавершение", ЭтотОбъект);

	ОткрытьФорму("Справочник.ДоговорыКонтрагентов.Форма.ОтправкаДокументовПоЭлектроннойПочте",
		ПараметрыФормы,
		ЭтотОбъект,
		,
		,
		,
		ОписаниеОповещения);

КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФайл(Команда)

	ОписаниеОповещения = Новый ОписаниеОповещения("ОткрытьФайлПослеУстановкиРасширенияРаботыСФайлами", ЭтотОбъект);

	Если ЭтоДополнительноеСоглашение Тогда
		ТекстСообщения = НСтр("ru = 'Для открытия текст дополнительного соглашения из внешнего файла рекомендуется установить расширение для веб-клиента 1С:Предприятие.'");
	Иначе
		ТекстСообщения = НСтр("ru = 'Для открытия текст договора из внешнего файла рекомендуется установить расширение для веб-клиента 1С:Предприятие.'");
	КонецЕсли;

	ОбщегоНазначенияКлиент.ПоказатьВопросОбУстановкеРасширенияРаботыСФайлами(ОписаниеОповещения, ТекстСообщения, Ложь);

КонецПроцедуры

&НаКлиенте
Процедура ЗаполнитьПоШаблону(Команда)
	
	// Покажем форму для выбора шаблона, по которому пользователь хочет перезаполнить текст договора.
	
	ОбъектПечатиДоговор = ТипЗнч(ОбъектПечати) = Тип("СправочникСсылка.ДоговорыКонтрагентов")
		И Не ЭтоДополнительноеСоглашение;
	ПараметрыОткрытия = Новый Структура("ОбъектПечатиДоговор", ОбъектПечатиДоговор);
	
	ОписаниеОповещения = Новый ОписаниеОповещения("ЗаполнитьПоШаблонуПослеВыбора", ЭтотОбъект);

	ОткрытьФорму("Справочник.ШаблоныДоговоров.ФормаВыбора", 
		ПараметрыОткрытия,
		ЭтотОбъект,
		,
		,
		,
		ОписаниеОповещения,
		РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);

КонецПроцедуры

#Область РеквизитыОрганизации

// Общие реквизиты организации

&НаКлиенте
Процедура ВставитьПолноеНаименованиеОрганизации(Команда)
	ВставитьПараметр("ПолноеНаименованиеОрганизации");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьБанковскиеРеквизитыОрганизации(Команда)
	ВставитьПараметр("БанковскиеРеквизитыОрганизации");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьИННКППОрганизации(Команда)
	ВставитьПараметр("ИНН_КППОрганизации");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьОГРНОрганизации(Команда)
	ВставитьПараметр("ОГРНОрганизации");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьДатуРегистрацииОрганизации(Команда)
	ВставитьПараметр("ДатаРегистрацииОрганизации");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьСвидетельствоОрганизации(Команда)
	ВставитьПараметр("СвидетельствоОрганизации");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьСловоИменуемоеДляОрганизации(Команда)
	ВставитьПараметр("СловоИменуемоеДляОрганизации");
КонецПроцедуры

// Контактная информация организации

&НаКлиенте
Процедура ВставитьЮридическийАдресОрганизации(Команда)
	ВставитьПараметр("ЮридическийАдресОрганизации");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьГородПоЮридическомуАдресуОрганизации(Команда)
	ВставитьПараметр("ГородПоЮридическомуАдресуОрганизации");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьФактическийАдресОрганизации(Команда)
	ВставитьПараметр("ФактическийАдресОрганизации");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьГородПоФактическомуАдресуОрганизации(Команда)
	ВставитьПараметр("ГородПоФактическомуАдресуОрганизации");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьПочтовыйАдресОрганизации(Команда)
	ВставитьПараметр("ПочтовыйАдресОрганизации");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьГородПоПочтовомуАдресуОрганизации(Команда)
	ВставитьПараметр("ГородПоПочтовомуАдресуОрганизации");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьТелефонОрганизации(Команда)
	ВставитьПараметр("ТелефонОрганизации");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьФаксОрганизации(Команда)
	ВставитьПараметр("ФаксОрганизации");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьАдресЭлектроннойПочтыОрганизации(Команда)
	ВставитьПараметр("АдресЭлектроннойПочтыОрганизации");
КонецПроцедуры

// Представитель организации

&НаКлиенте
Процедура ВставитьПредставительОрганизации(Команда)
	ВставитьПараметр("ПредставительОрганизации");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьРуководительОрганизации(Команда)
	ВставитьПараметр("РуководительОрганизации");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьДолжностьРуководителяОрганизации(Команда)
	ВставитьПараметр("ДолжностьРуководителяОрганизации");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьОснованиеПолномочийРуководителяОрганизации(Команда)
	ВставитьПараметр("ОснованиеПолномочийРуководителяОрганизации");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьРуководительОрганизацииРодительныйПадеж(Команда)
	ВставитьПараметр("РуководительОрганизацииРодительныйПадеж");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьРуководительОрганизацииДательныйПадеж(Команда)
	ВставитьПараметр("РуководительОрганизацииДательныйПадеж");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьРуководительОрганизацииВинительныйПадеж(Команда)
	ВставитьПараметр("РуководительОрганизацииВинительныйПадеж");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьРуководительОрганизацииТворительныйПадеж(Команда)
	ВставитьПараметр("РуководительОрганизацииТворительныйПадеж");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьРуководительОрганизацииПредложныйПадеж(Команда)
	ВставитьПараметр("РуководительОрганизацииПредложныйПадеж");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьДолжностьРуководителяОрганизацииРодительныйПадеж(Команда)
	ВставитьПараметр("ДолжностьРуководителяОрганизацииРодительныйПадеж");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьДолжностьРуководителяОрганизацииДательныйПадеж(Команда)
	ВставитьПараметр("ДолжностьРуководителяОрганизацииДательныйПадеж");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьДолжностьРуководителяОрганизацииВинительныйПадеж(Команда)
	ВставитьПараметр("ДолжностьРуководителяОрганизацииВинительныйПадеж");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьДолжностьРуководителяОрганизацииТворительныйПадеж(Команда)
	ВставитьПараметр("ДолжностьРуководителяОрганизацииТворительныйПадеж");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьДолжностьРуководителяОрганизацииПредложныйПадеж(Команда)
	ВставитьПараметр("ДолжностьРуководителяОрганизацииПредложныйПадеж");
КонецПроцедуры

#КонецОбласти

#Область РеквизитыКонтрагента

// Общие реквизиты контрагента

&НаКлиенте
Процедура ВставитьПолноеНаименованиеКонтрагента(Команда)
	ВставитьПараметр("ПолноеНаименованиеКонтрагента");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьБанковскиеРеквизитыКонтрагента(Команда)
	ВставитьПараметр("БанковскиеРеквизитыКонтрагента");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьИННКППКонтрагента(Команда)
	ВставитьПараметр("ИНН_КППКонтрагента");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьОГРНКонтрагента(Команда)
	ВставитьПараметр("ОГРНКонтрагента");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьДатуРегистрацииКонтрагента(Команда)
	ВставитьПараметр("ДатаРегистрацииКонтрагента");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьСвидетельствоКонтрагента(Команда)
	ВставитьПараметр("СвидетельствоКонтрагента");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьДокументУдостоверяющийЛичностьКонтрагента(Команда)
	ВставитьПараметр("ДокументУдостоверяющийЛичностьКонтрагента");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьСловоИменуемоеДляКонтрагента(Команда)
	ВставитьПараметр("СловоИменуемоеДляКонтрагента");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьУсловиеИзменилсяСтатусСамозанятого(Команда)
	ВставитьПараметр("УсловиеИзменилсяСтатусСамозанятого");
КонецПроцедуры

// Контактная информация контрагента

&НаКлиенте
Процедура ВставитьЮридическийАдресКонтрагента(Команда)
	ВставитьПараметр("ЮридическийАдресКонтрагента");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьГородПоЮридическомуАдресуКонтрагента(Команда)
	ВставитьПараметр("ГородПоЮридическомуАдресуКонтрагента");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьФактическийАдресКонтрагента(Команда)
	ВставитьПараметр("ФактическийАдресКонтрагента");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьГородПоФактическомуАдресуКонтрагента(Команда)
	ВставитьПараметр("ГородПоФактическомуАдресуКонтрагента");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьПочтовыйАдресКонтрагента(Команда)
	ВставитьПараметр("ПочтовыйАдресКонтрагента");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьГородПоПочтовомуАдресуКонтрагента(Команда)
	ВставитьПараметр("ГородПоПочтовомуАдресуКонтрагента");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьТелефонКонтрагента(Команда)
	ВставитьПараметр("ТелефонКонтрагента");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьФаксКонтрагента(Команда)
	ВставитьПараметр("ФаксКонтрагента");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьАдресЭлектроннойПочтыКонтрагента(Команда)
	ВставитьПараметр("АдресЭлектроннойПочтыКонтрагента");
КонецПроцедуры

// Представитель контрагента

&НаКлиенте
Процедура ВставитьПредставительКонтрагента(Команда)
	ВставитьПараметр("ПредставительКонтрагента");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьРуководительКонтрагента(Команда)
	ВставитьПараметр("РуководительКонтрагента");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьДолжностьРуководителяКонтрагента(Команда)
	ВставитьПараметр("ДолжностьРуководителяКонтрагента");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьОснованиеПолномочийРуководителяКонтрагента(Команда)
	ВставитьПараметр("ОснованиеПолномочийРуководителяКонтрагента");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьРуководительКонтрагентаРодительныйПадеж(Команда)
	ВставитьПараметр("РуководительКонтрагентаРодительныйПадеж");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьРуководительКонтрагентаДательныйПадеж(Команда)
	ВставитьПараметр("РуководительКонтрагентаДательныйПадеж");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьРуководительКонтрагентаВинительныйПадеж(Команда)
	ВставитьПараметр("РуководительКонтрагентаВинительныйПадеж");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьРуководительКонтрагентаТворительныйПадеж(Команда)
	ВставитьПараметр("РуководительКонтрагентаТворительныйПадеж");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьРуководительКонтрагентаПредложныйПадеж(Команда)
	ВставитьПараметр("РуководительКонтрагентаПредложныйПадеж");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьДолжностьРуководителяКонтрагентаРодительныйПадеж(Команда)
	ВставитьПараметр("ДолжностьРуководителяКонтрагентаРодительныйПадеж");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьДолжностьРуководителяКонтрагентаДательныйПадеж(Команда)
	ВставитьПараметр("ДолжностьРуководителяКонтрагентаДательныйПадеж");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьДолжностьРуководителяКонтрагентаВинительныйПадеж(Команда)
	ВставитьПараметр("ДолжностьРуководителяКонтрагентаВинительныйПадеж");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьДолжностьРуководителяКонтрагентаТворительныйПадеж(Команда)
	ВставитьПараметр("ДолжностьРуководителяКонтрагентаТворительныйПадеж");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьДолжностьРуководителяКонтрагентаПредложныйПадеж(Команда)
	ВставитьПараметр("ДолжностьРуководителяКонтрагентаПредложныйПадеж");
КонецПроцедуры

#КонецОбласти

#Область РеквизитыДоговора

&НаКлиенте
Процедура ВставитьДатуДоговора(Команда)
	ВставитьПараметр("ДатаДоговора");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьНомерДоговора(Команда)
	ВставитьПараметр("НомерДоговора");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьСрокДействия(Команда)
	ВставитьПараметр("СрокДействияДоговора");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьУсловиеОплаты(Команда)
	ВставитьПараметр("УсловиеОплаты");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьСрокОплаты(Команда)
	ВставитьПараметр("СрокОплатыПоДоговору");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьДатуОплатыДоговора(Команда)
	ВставитьПараметр("ДатаОплатыПоДоговору");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьСуммаДоговора(Команда)
	ВставитьПараметр("СуммаДоговора");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьСуммаДоговораПрописью(Команда)
	ВставитьПараметр("СуммаДоговораПрописью");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьСуммаДоговораБезНДС(Команда)
	ВставитьПараметр("СуммаДоговораБезНДС");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьСуммаДоговораБезНДСПрописью(Команда)
	ВставитьПараметр("СуммаДоговораБезНДСПрописью");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьСуммаНДСДоговора(Команда)
	ВставитьПараметр("СуммаНДСДоговора");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьСуммаНДСДоговораПрописью(Команда)
	ВставитьПараметр("СуммаНДСДоговораПрописью");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьСуммаСНДСДоговораПрописью(Команда)
	ВставитьПараметр("СуммаСНДСДоговораПрописью");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьСтавкаНДСДоговора(Команда)
	ВставитьПараметр("СтавкаНДСДоговора");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьСтавкаНДСДоговораПрописью(Команда)
	ВставитьПараметр("СтавкаНДСДоговораПрописью");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьПредметДоговора(Команда)
	
	ВставитьПараметр("ПредметДоговора");
	
КонецПроцедуры

#КонецОбласти

#Область РеквизитыСчетаНаОплату

&НаКлиенте
Процедура ВставитьСуммуДокумента(Команда)
	ВставитьПараметр("СуммаДокумента");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьСуммаДокументаПрописью(Команда)
	ВставитьПараметр("СуммаДокументаПрописью");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьСуммаДокументаБезНДС(Команда)
	ВставитьПараметр("СуммаДокументаБезНДС");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьСуммаДокументаБезНДСПрописью(Команда)
	ВставитьПараметр("СуммаДокументаБезНДСПрописью");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьСуммаНДС(Команда)
	ВставитьПараметр("СуммаНДС");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьСуммаНДСПрописью(Команда)
	ВставитьПараметр("СуммаНДСПрописью");
КонецПроцедуры

#КонецОбласти

&НаКлиенте
Процедура ВставитьДополнительныйРеквизит(Команда)
	СтрокаИндексПараметра = СтрЗаменить(Команда.Имя, "ВставитьДополнительныйРеквизит", "");
	ИндексПараметра = ?(ПустаяСтрока(СтрокаИндексПараметра),0,Число(СтрокаИндексПараметра));
	ВставитьПараметр(ЭтаФорма.ДополнительныеРеквизиты[ИндексПараметра].Значение);
КонецПроцедуры

&НаКлиенте
Процедура ВставитьДатуСоглашения(Команда)
	ВставитьПараметр("ДатаСоглашения");
КонецПроцедуры

&НаКлиенте
Процедура ВставитьНомерСоглашения(Команда)
	ВставитьПараметр("НомерСоглашения");
КонецПроцедуры

&НаКлиенте
Процедура ОтправитьДополнительноеСоглашениеПоЭДО(Команда)
	
	Если Модифицированность Или Не ЗначениеЗаполнено(ФайлДоговора) Тогда
		СохранитьТекстДоговораВФайлДоговора();
	КонецЕсли;
	
	ПараметрыФормы = Новый Структура;
	ПараметрыФормы.Вставить("ОбъектОтправки", ОбъектПечати);
	ОткрытьФорму("Справочник.ДоговорыКонтрагентов.Форма.ОтправкаДополнительногоСоглашенияПоЭДО", ПараметрыФормы, ЭтотОбъект);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

#Область ОбщиеПроцедурыИФункцииУправленияФормой

&НаСервере
Процедура ЗагрузитьСведенияОДоговореКонтрагента()

	Если ТипЗнч(ОбъектПечати) = Тип("СправочникСсылка.ДоговорыКонтрагентов") Тогда
		ДоговорКонтрагента = ОбъектПечати;
	ИначеЕсли ОбъектПечати.Метаданные().Реквизиты.Найти("ДоговорКонтрагента") <> Неопределено Тогда
		ДоговорКонтрагента = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ОбъектПечати, "ДоговорКонтрагента");
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ДоговорКонтрагента) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Не определен договор для %1'"), ОбъектПечати);
		ВызватьИсключение ТекстСообщения;
	КонецЕсли;

	РеквизитыДоговора = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(ДоговорКонтрагента,
		"Владелец, ФайлДоговора, ДоговорПодписан, Номер, Дата");
	
	Контрагент = РеквизитыДоговора.Владелец;
	Если ИспользоватьФайлДоговора Тогда
		ФайлДоговора = РеквизитыДоговора.ФайлДоговора;
	КонецЕсли;
	НомерДоговора = РеквизитыДоговора.Номер;
	ДатаДоговора = РеквизитыДоговора.Дата;
	// Считаем, что договор подписан, только если создан файл договора
	ДоговорПодписан = РеквизитыДоговора.ДоговорПодписан И ЗначениеЗаполнено(ФайлДоговора);
	
КонецПроцедуры 

&НаСервере
Процедура УправлениеФормой()
	
	ВидимостьКоманднаяПанель = Истина;
	
	Если ПодписанЭП Тогда
		ВидимостьКоманднаяПанель = Ложь;
		СтатусДоговора = ?(ЭтоДополнительноеСоглашение,
			НСтр("ru = 'Дополнительное соглашение подписано электронной подписью'"),
			НСтр("ru = 'Договор подписан электронной подписью'"));
	ИначеЕсли ДоговорПодписан Тогда
		ВидимостьКоманднаяПанель = Ложь;
		СтатусДоговора = ?(ЭтоДополнительноеСоглашение,
			НСтр("ru = 'Дополнительное соглашение подписано'"),
			НСтр("ru = 'Договор подписан'"));
	КонецЕсли;
	
	Элементы.ГруппаКоманднаяПанель.Видимость = ВидимостьКоманднаяПанель;
	Элементы.ГруппаПодписан.Видимость = НЕ ВидимостьКоманднаяПанель;
	
	Если ЭтоДополнительноеСоглашение Тогда
		РеквизитыСоглашенияСтрокой = ПечатьДоговоровКлиентСервер.РеквизитыДоговораСтрокой(НомерСоглашения, ДатаСоглашения);
		Заголовок = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Дополнительное соглашение %1 (%2)'"), РеквизитыСоглашенияСтрокой, Контрагент);
	Иначе
		Заголовок = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = '%1 (%2)'"), ДоговорКонтрагента, Контрагент);
	КонецЕсли;
	
	Элементы.ФормаПечать.Видимость = Не ЭлектронныйФормат;
	Элементы.ПросмотрИПечатьЭД.Видимость = ЭлектронныйФормат;
	Элементы.ГруппаСостояниеЭДО.Видимость = ЭлектронныйФормат;
	
	БаннерПодсказкаБылСкрыт = ОбщегоНазначения.ХранилищеОбщихНастроекЗагрузить(
			"ДоговорыКонтрагентов", "БаннерПодсказкаСкрыт");
	ПоказатьБаннерБаннерПодсказка = БаннерПодсказкаБылСкрыт <> Истина;
	Элементы.ГруппаБаннерПодсказка.Видимость = ЭлектронныйФормат И ПоказатьБаннерБаннерПодсказка И Не ДоговорПодписан;
	
	Если Справочники.Контрагенты.ЭтоИндивидуальныйПредприниматель(Контрагент) Тогда
		Элементы.ВставитьСвидетельствоКонтрагента.Видимость = Не РегистрацияОрганизацииКлиентСервер.ВыдаетсяЛистЗаписиЕГР(
			ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Контрагент, "ДатаРегистрации"));
	Иначе
		Элементы.ВставитьСвидетельствоКонтрагента.Видимость = Ложь;
	КонецЕсли;
	
	Организация = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ОбъектПечати, "Организация");
	Если ЗначениеЗаполнено(Организация) И Не ОбщегоНазначенияБПВызовСервераПовтИсп.ЭтоЮрЛицо(Организация) Тогда
		Элементы.ВставитьСвидетельствоОрганизации.Видимость = Не РегистрацияОрганизацииКлиентСервер.ВыдаетсяЛистЗаписиЕГР(
			ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Организация, "ДатаРегистрации"));
	Иначе
		Элементы.ВставитьСвидетельствоОрганизации.Видимость = Ложь;
	КонецЕсли;
	
	Элементы.ГруппаПараметрыСоглашения.Видимость = ЭтоДополнительноеСоглашение;
	
	Элементы.ФормаОтправитьДополнительноеСоглашениеПоЭДО.Видимость = ЭтоДополнительноеСоглашение
		И ОбменСКонтрагентами.ЕстьПравоВыполненияОбмена();
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОбработчикОжиданияПолучитьИЗагрузитьФайлИзХранилища()
	
	ПараметрыЗавершенияРедактирования = Новый Структура();
	ПараметрыЗавершенияРедактирования.Вставить("ФайлДоговора", ФайлДоговора);
	ОписаниеОповещения = Новый ОписаниеОповещения("ЗакончитьРедактированиеВыполненоПомещение", ЭтотОбъект, ПараметрыЗавершенияРедактирования);
	
	РаботаСФайламиКлиент.ПоместитьПрисоединенныйФайл(ОписаниеОповещения, ФайлДоговора, УникальныйИдентификатор);
	
КонецПроцедуры

&НаКлиенте
Процедура ПередЗакрытиемЗавершение(РезультатВопроса, ДополнительныеПараметры) Экспорт
	
	Если РезультатВопроса = КодВозвратаДиалога.Да Тогда
		
		ФорматыСохранения = Новый Массив;
		ФорматыСохранения.Добавить(ТипФайлаТабличногоДокумента.HTML);
		ЗаписатьФайлДоговораНаКлиенте();
		Закрыть();
	
	ИначеЕсли РезультатВопроса = КодВозвратаДиалога.Нет Тогда
		
		РазблокироватьФайлНаСервере();
	
		Модифицированность = Ложь;
		Закрыть();
	
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФайлПослеУстановкиРасширенияРаботыСФайлами(РасширениеПодключено, ДополнительныеПараметры) Экспорт

	Если НЕ РасширениеПодключено Тогда
		Возврат;
	КонецЕсли;
	
	ТекущийТекст = ФорматированныйДокумент.ПолучитьТекст(ФорматированныйДокумент.ПолучитьЗакладкуНачала(), ФорматированныйДокумент.ПолучитьЗакладкуКонца());
	Если ЗначениеЗаполнено(ТекущийТекст) Тогда
	
		ОписаниеОповещения = Новый ОписаниеОповещения("ОткрытьФайлПослеВопросаОЗаменеЗавершение", ЭтотОбъект);
		
		Если ЭтоДополнительноеСоглашение Тогда
			ТекстВопроса = НСтр("ru = 'Текст дополнительного соглашения будет заменен на текст из файла. Продолжить?'");
		Иначе
			ТекстВопроса = НСтр("ru = 'Текст договора будет заменен на текст из файла. Продолжить?'");
		КонецЕсли;
		
		ПоказатьВопрос(ОписаниеОповещения, ТекстВопроса, РежимДиалогаВопрос.ДаНет, , КодВозвратаДиалога.Нет);
		
	Иначе
	
		ОткрытьФайлНаКлиенте();
		
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФайлПослеВопросаОЗаменеЗавершение(РезультатВопроса, ДополнительныеПараметры) Экспорт

	Если РезультатВопроса = КодВозвратаДиалога.Да Тогда
	
		ОткрытьФайлНаКлиенте();
	
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФайлНаКлиенте()

	ОписаниеОповещения = Новый ОписаниеОповещения("ОткрытьФайлПослеПомещенияВоВременноеХранилище", ЭтотОбъект);

	НачатьПомещениеФайла(ОписаниеОповещения, , , Истина, УникальныйИдентификатор); 

КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФайлПослеПомещенияВоВременноеХранилище(Результат, АдресХранилища, ВыбранноеИмяФайла, ДополнительныеПараметры) Экспорт

	Если Результат = Ложь Тогда
		Возврат
	КонецЕсли;
	
	ОткрытьФайлНаСервере(АдресХранилища);

КонецПроцедуры

&НаСервере
Процедура ОткрытьФайлНаСервере(Знач АдресХранилища)

	ДвоичныеДанные = ПолучитьИзВременногоХранилища(АдресХранилища);
	
	ИмяВременногоФайла = ПолучитьИмяВременногоФайла("htm");
	ДвоичныеДанные.Записать(ИмяВременногоФайла);
	
	ТекстовыйДокументHTML = Новый ТекстовыйДокумент;
	ТекстовыйДокументHTML.Прочитать(ИмяВременногоФайла, КодировкаТекста.UTF8);
	
	СтруктураHTML = СтруктураHTMLСКартинками(ТекстовыйДокументHTML.ПолучитьТекст());
	
	ФорматированныйДокумент.УстановитьHTML(СтруктураHTML.ТекстHTML, СтруктураHTML.Вложения);

	Модифицированность = Истина;

КонецПроцедуры

&НаКлиенте
Процедура ЗаполнитьПоШаблонуПослеВыбора(РезультатЗакрытия, ДополнительныеПараметры) Экспорт

	Если Не ЗначениеЗаполнено(РезультатЗакрытия) Тогда
		Возврат;
	КонецЕсли;
	
	Если ТипЗнч(РезультатЗакрытия) = Тип("Структура") Тогда
		Шаблон = РезультатЗакрытия.Шаблон;
		ЭлектронныйФормат = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(РезультатЗакрытия, "ЭлектронныйФормат", Ложь);
	Иначе
		Шаблон = РезультатЗакрытия;
		ЭлектронныйФормат = Ложь;
	КонецЕсли;

	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ШаблонДоговора", Шаблон);
	ДополнительныеПараметры.Вставить("ЭлектронныйФормат",  ЭлектронныйФормат);

	ОписаниеОповещения = Новый ОписаниеОповещения("ЗаполнитьПоШаблонуПослеПодтверждения", 
		ЭтотОбъект, ДополнительныеПараметры);

	Если ЭтоДополнительноеСоглашение Тогда
		ТекстВопроса = НСтр("ru = 'Заменить текущий текст дополнительного соглашения на текст из шаблона ""%1""?'");
	Иначе
		ТекстВопроса = НСтр("ru = 'Заменить текущий текст договора на текст из шаблона ""%1""?'");
	КонецЕсли;
	ТекстВопроса = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(ТекстВопроса, Шаблон);
	
	ПоказатьВопрос(ОписаниеОповещения, ТекстВопроса, РежимДиалогаВопрос.ДаНет, , КодВозвратаДиалога.Нет);

КонецПроцедуры

&НаКлиенте
Процедура ЗаполнитьПоШаблонуПослеПодтверждения(РезультатВопроса, ДополнительныеПараметры) Экспорт

	Если РезультатВопроса <> КодВозвратаДиалога.Да Тогда
		Возврат;
	КонецЕсли;

	ФорматированныйДокумент.Удалить();
	
	ШаблонДоговора = ДополнительныеПараметры.ШаблонДоговора;
	ЭлектронныйФормат = ДополнительныеПараметры.ЭлектронныйФормат;
	ЗагрузитьШаблонДоговора();
	Модифицированность = Истина;
	
	ПодключитьОбработчикОжидания("Подключаемый_ОжидатьЗагрузкиШаблона", 0.1, Истина);

КонецПроцедуры

&НаСервере
Процедура ЗакрытьБаннерПодсказка()
	
	Элементы.ГруппаБаннерПодсказка.Видимость = Ложь;
	ОбщегоНазначения.ХранилищеОбщихНастроекСохранить(
		"ДоговорыКонтрагентов", "БаннерПодсказкаСкрыт", Истина);
	
КонецПроцедуры

#КонецОбласти

#Область РаботаСШаблоном

&НаКлиенте
Процедура ВставитьПараметр(ИмяПараметра)
	
	// Значения выводим жирным шрифтом.

	ЗакладкаНачала = Неопределено;
	ЗакладкаОкончания = Неопределено;
	Элементы.ФорматированныйДокумент.ПолучитьГраницыВыделения(ЗакладкаНачала, ЗакладкаОкончания);

	ПозицияНачала = ФорматированныйДокумент.ПолучитьПозициюПоЗакладке(ЗакладкаНачала);
	ПозицияОкончания = ФорматированныйДокумент.ПолучитьПозициюПоЗакладке(ЗакладкаОкончания);

	ШрифтПоУмолчанию = Неопределено;
	ЦветФонаПоУмолчанию = Неопределено;
	ВыделенныеЭлементы = ФорматированныйДокумент.ПолучитьЭлементы(ЗакладкаНачала, ЗакладкаОкончания);
	// Ищем последний текстовый элемент среди выделенных для копирования его шрифта.
	Сч = ВыделенныеЭлементы.Количество() - 1;
	Пока Сч >= 0 Цикл
		Если ТипЗнч(ВыделенныеЭлементы[Сч]) = Тип("ТекстФорматированногоДокумента") Тогда
			ШрифтПоУмолчанию 	= ВыделенныеЭлементы[Сч].Шрифт;
			ЦветФонаПоУмолчанию = ВыделенныеЭлементы[Сч].ЦветФона;
			Прервать;
		КонецЕсли;
		Сч = Сч - 1;
	КонецЦикла;
	
	Если ШрифтПоУмолчанию = Неопределено Тогда
		ШрифтПоУмолчанию = Новый Шрифт();
	КонецЕсли;	

	Если ЦветФонаПоУмолчанию = Неопределено Тогда
		ЦветФонаПоУмолчанию = ПечатьДоговоровКлиентСервер.ЦветФонаПоУмолчанию();
	КонецЕсли;
		
	Если ЗакладкаНачала <> ЗакладкаОкончания Тогда 
		ФорматированныйДокумент.Удалить(ЗакладкаНачала, ЗакладкаОкончания);
		Элементы.ФорматированныйДокумент.УстановитьГраницыВыделения(ЗакладкаНачала, ЗакладкаНачала);
	КонецЕсли;
	
	РеквизитыДополнительногоСоглашения = РеквизитыДополнительногоСоглашения(НомерСоглашения, ДатаСоглашения);
	
	// Чтобы не передавать с клиент на сервер весь форматированный документ, 
	// подгототавливаем в функции новый документ только с текстом параметра,
	// а здесь переносим его элементы в документ на форме.
	ВременныйФорматированныйДокумент = ЗаменитьПараметрНаСервере(
		ИмяПараметра, ОбъектПечати, ШаблонДоговора, ШрифтПоУмолчанию, ЭлектронныйФормат, РеквизитыДополнительногоСоглашения);
	
	ДобавленныеЭлементы = ВременныйФорматированныйДокумент.ПолучитьЭлементы();
	ДлинаТекста = 0;
	Для Каждого Элемент Из ДобавленныеЭлементы Цикл
		// в веб-клиенте добавляется ПереводСтроки последний элементом. Его не нужно обрабатывать.
		Если ТипЗнч(Элемент) <> Тип("ТекстФорматированногоДокумента") Тогда
			Продолжить;
		КонецЕсли;
		НовыйЭлемент = ФорматированныйДокумент.Вставить(ЗакладкаНачала, Элемент.Текст, Тип("ТекстФорматированногоДокумента"));
		Если НовыйЭлемент <> Неопределено Тогда
			ДлинаТекста = ДлинаТекста + СтрДлина(НовыйЭлемент.Текст);
			Если Элемент.Шрифт.Полужирный = Истина Тогда
				НовыйЭлемент.Шрифт = Новый Шрифт(НовыйЭлемент.Шрифт, , , Истина);
			Иначе
				НовыйЭлемент.Шрифт = Новый Шрифт(НовыйЭлемент.Шрифт, , , Ложь);
			КонецЕсли;
			
			Если Лев(Элемент.Текст, 1) = "_" Тогда
				НовыйЭлемент.ЦветФона = ЦветФонаЗаменяемогоПараметра;
			Иначе
				НовыйЭлемент.ЦветФона = ЦветФонаПоУмолчанию;
			КонецЕсли;
			
			Если ЭлектронныйФормат Тогда 
				Если СтрНайти(НовыйЭлемент.Текст, "[#") > 0 Или СтрНайти(НовыйЭлемент.Текст, "#]") > 0 Тогда
					НовыйЭлемент.ЦветТекста = ОбщегоНазначенияКлиент.ЦветСтиля("ЦветМеткиСерый");
					// Для корректного отображения в HTML зададим абсолютное значение
					НовыйЭлемент.Шрифт = Новый Шрифт(НовыйЭлемент.Шрифт, , 8, Ложь);
				КонецЕсли;
			КонецЕсли;
			
			ЗакладкаНачала = НовыйЭлемент.ЗакладкаКонца;
		КонецЕсли;
	КонецЦикла;
	
	ПозицияОкончания = ПозицияНачала + ДлинаТекста;
	ЗакладкаОкончания = ФорматированныйДокумент.ПолучитьЗакладкуПоПозиции(ПозицияОкончания);
	Элементы.ФорматированныйДокумент.УстановитьГраницыВыделения(ЗакладкаНачала, ЗакладкаОкончания);
	
	Модифицированность = Истина;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ЗаменитьПараметрНаСервере(Знач ИмяПараметра, Знач ОбъектПечати, Знач ШаблонДоговора, Знач ШрифтПоУмолчанию, Знач ЭлектронныйФормат, РеквизитыСоглашения)

	Результат = Новый ФорматированныйДокумент;
	
	СведенияОСделке = ПечатьДоговоров.СведенияОСделке(ОбъектПечати, ШаблонДоговора, РеквизитыСоглашения);
	
	ЗначенияСоставногоПараметра = Новый Массив;
	
	Успешно = Истина;
	
	Значение = ПечатьДоговоров.ЗначениеПараметра(ИмяПараметра, СведенияОСделке, ЗначенияСоставногоПараметра, Успешно, ЭлектронныйФормат, РеквизитыСоглашения);
	
	ПервыйЭлементДокумента = Результат.Добавить(" ", Тип("ТекстФорматированногоДокумента"));
	Если СтрНачинаетсяС(ИмяПараметра, "Слово")
		Или ИмяПараметра = "ПредметДоговора" Тогда
		ПервыйЭлементДокумента.Шрифт = ШрифтПоУмолчанию;
	Иначе
		ПервыйЭлементДокумента.Шрифт = Новый Шрифт(ШрифтПоУмолчанию, , , Истина);
	КонецЕсли;
	ПервыйЭлементДокумента.ЦветФона = ПечатьДоговоров.ЦветФонаЗаменяемогоПараметра();
	
	Если СтрНайти(Значение, "%1%") > 0 Тогда
		ПечатьДоговоров.ВставитьСоставноеЗначение(
			Значение,
			ЗначенияСоставногоПараметра,
			Результат,
			ПервыйЭлементДокумента);
	Иначе
		НовыйЭлемент = Результат.Добавить(Значение, Тип("ТекстФорматированногоДокумента"));
		Если НовыйЭлемент <> Неопределено Тогда
			НовыйЭлемент.Шрифт = ПервыйЭлементДокумента.Шрифт;
		КонецЕсли;
	КонецЕсли;
	
	// Удалим пустую в начале процедуры.
	Результат.Удалить(ПервыйЭлементДокумента.ЗакладкаНачала, ПервыйЭлементДокумента.ЗакладкаКонца);
		
	Возврат Результат;

КонецФункции

&НаСервере
Процедура ЗагрузитьШаблонДоговора()
	
	ПараметрыЗаполнения = ПечатьДоговоров.СтруктураПараметровПечатиДоговора();
	ПараметрыЗаполнения.ШаблонДоговора = ШаблонДоговора;
	ПараметрыЗаполнения.СсылкаНаОбъект = ОбъектПечати;
	ПараметрыЗаполнения.ЭлектронныйФормат = ЭлектронныйФормат;
	Если ЭтоДополнительноеСоглашение Тогда
		ПараметрыЗаполнения.РеквизитыДополнительногоСоглашения = РеквизитыДополнительногоСоглашения(
			НомерСоглашения, ДатаСоглашения);
	КонецЕсли;

	ПараметрыВыполненияВФоне = ДлительныеОперации.ПараметрыВыполненияВФоне(УникальныйИдентификатор);
	ПараметрыВыполненияВФоне.НаименованиеФоновогоЗадания = НСтр("ru = 'Заполнение шаблона договора'");
	ПараметрыВыполненияВФоне.КлючФоновогоЗадания         = ОбъектПечати;
	ДлительнаяОперация = ДлительныеОперации.ВыполнитьВФоне("ПечатьДоговоров.ЗаполнитьТекстДоговораПоШаблонуВФоне",
															ПараметрыЗаполнения, ПараметрыВыполненияВФоне);
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОжидатьЗагрузкиШаблона()
	
	ОповещениеОЗавершении = Новый ОписаниеОповещения("ОбработатьЗагрузкуШаблона", ЭтотОбъект);
	
	ПараметрыОжидания = ДлительныеОперацииКлиент.ПараметрыОжидания(ЭтотОбъект);
	ПараметрыОжидания.ТекстСообщения = НСтр("ru = 'Заполнение шаблона договора'");
	ПараметрыОжидания.ВыводитьОкноОжидания = Истина;
	ПараметрыОжидания.ВыводитьСообщения = Истина;
	
	ДлительныеОперацииКлиент.ОжидатьЗавершение(ДлительнаяОперация, ОповещениеОЗавершении, ПараметрыОжидания);
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработатьЗагрузкуШаблона(Результат, ДополнительныеПараметры) Экспорт
	
	ДлительнаяОперация = Неопределено;
	
	Если Результат = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если Результат.Статус <> "Выполнено" Тогда
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(
			Результат.КраткоеПредставлениеОшибки,
			ШаблонДоговора,
			"ФорматированныйДокумент");
		Возврат;
	КонецЕсли;
	
	ОбработатьЗагрузкуШаблонаНаСервере(Результат.АдресРезультата);
	
КонецПроцедуры

&НаСервере
Процедура ОбработатьЗагрузкуШаблонаНаСервере(Знач АдресХранилища)

	// Получаем из фонового задания подготовленный текст и вложения
	// и заполняем по ним форматированный документ.
	Если ЭтоАдресВременногоХранилища(АдресХранилища) Тогда
		Результат = ПолучитьИзВременногоХранилища(АдресХранилища);
		Если ТипЗнч(Результат) = Тип("ХранилищеЗначения") Тогда
			ДанныеДокумента = Результат.Получить();
		КонецЕсли;
	КонецЕсли;

	Если ТипЗнч(ДанныеДокумента) = Тип("Структура") Тогда
		ФорматированныйДокумент.УстановитьHTML(ДанныеДокумента.ТекстHTML, ДанныеДокумента.Вложения);
	Иначе
		ФорматированныйДокумент.УстановитьHTML("", Новый Структура);
	КонецЕсли;

КонецПроцедуры

&НаСервере
Процедура ДобавитьДополнительныеРеквизитыИДопСвойства()
	
	// Добавим дополнительные реквизиты и доп. свойства объектов для выбора в качестве параметров.
	
	СоответствиеДопРеквизитыГруппаЭлементов = Новый Соответствие;
	
	ВсеСвойства = Новый Массив;
	
	// Список доп. реквизитов и доп. сведений организации
	СвойстваОбъекта = УправлениеСвойствами.СвойстваОбъекта(Справочники.Организации.ПустаяСсылка(), Истина, Истина);
	ОбщегоНазначенияКлиентСервер.ДополнитьМассив(ВсеСвойства, СвойстваОбъекта);
	СоответствиеДопРеквизитыГруппаЭлементов.Вставить(Элементы.ГруппаДополнительныеРеквизитыОрганизация, СвойстваОбъекта);
	
	// Список доп. реквизитов и доп. сведений контрагента
	СвойстваОбъекта = УправлениеСвойствами.СвойстваОбъекта(Справочники.Контрагенты.ПустаяСсылка(), Истина, Истина);
	ОбщегоНазначенияКлиентСервер.ДополнитьМассив(ВсеСвойства, СвойстваОбъекта);
	СоответствиеДопРеквизитыГруппаЭлементов.Вставить(Элементы.ГруппаДополнительныеРеквизитыКонтрагент, СвойстваОбъекта);
	
	// Список доп. реквизитов и доп. сведений договора
	СвойстваОбъекта = УправлениеСвойствами.СвойстваОбъекта(Справочники.ДоговорыКонтрагентов.ПустаяСсылка(), Истина, Истина);
	ОбщегоНазначенияКлиентСервер.ДополнитьМассив(ВсеСвойства, СвойстваОбъекта);
	СоответствиеДопРеквизитыГруппаЭлементов.Вставить(Элементы.ГруппаДополнительныеРеквизитыДоговорКонтрагента, СвойстваОбъекта);
	
	// Список доп. реквизитов и доп. сведений документа
	ЭтоСчетНаОплату = ТипЗнч(ОбъектПечати) = Тип("ДокументСсылка.СчетНаОплатуПокупателю");
	
	Если ЭтоСчетНаОплату Тогда
		СвойстваОбъекта = УправлениеСвойствами.СвойстваОбъекта(Документы.СчетНаОплатуПокупателю.ПустаяСсылка(), Ложь, Истина);
		ОбщегоНазначенияКлиентСервер.ДополнитьМассив(ВсеСвойства, СвойстваОбъекта);
		СоответствиеДопРеквизитыГруппаЭлементов.Вставить(Элементы.ГруппаДополнительныеРеквизитыДокумента, СвойстваОбъекта);
	Иначе
		Элементы.ГруппаПараметрыДокумента.Видимость = Ложь;
	КонецЕсли;
	
	// Текстовые поля по массиву ссылок
	РеквизитыСвойств = ОбщегоНазначения.ЗначенияРеквизитовОбъектов(ВсеСвойства, "Заголовок, Наименование");
	
	// Вставляем в подменю каждого объекта перечень всех его доп. реквизитов и доп. свойств отдельными пунктами.
	
	НомерКоманды = 0;
	Для Каждого КлючИЗначение Из СоответствиеДопРеквизитыГруппаЭлементов Цикл 
		
		МассивСвойств = КлючИЗначение.Значение;	
		
		Для Каждого СсылкаНаСвойство Из МассивСвойств Цикл
			
			ПоляСвойства = РеквизитыСвойств[СсылкаНаСвойство];
			Если ПоляСвойства = Неопределено Тогда
				Продолжить;
			КонецЕсли;
			
			ИмяКоманды = "ВставитьДополнительныйРеквизит" + Формат(НомерКоманды, "ЧН=0; ЧГ=");
			НаименованиеРеквизита = ПоляСвойства.Заголовок;
			
			Команда = Команды.Добавить(ИмяКоманды);
			Команда.Заголовок = НаименованиеРеквизита;
			Команда.Действие = "ВставитьДополнительныйРеквизит";
			Команда.ИзменяетСохраняемыеДанные = Истина;
			
			Кнопка = Элементы.Добавить(ИмяКоманды, Тип("КнопкаФормы"), КлючИЗначение.Ключ);
			Кнопка.ИмяКоманды = ИмяКоманды;
			Кнопка.Заголовок = НаименованиеРеквизита;
			
			ЭтотОбъект.ДополнительныеРеквизиты.Добавить(ПоляСвойства.Наименование);
			
			НомерКоманды = НомерКоманды + 1;
			
		КонецЦикла;
		
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти

#Область ОткрытиеИСохранениеФайлаДоговора

&НаСервере
Процедура ПроверитьФайлАктуаленИУстановитьТолькоПросмотр(Отказ)
	
	ДанныеФайла = ПолучитьДанныеФайла(ФайлДоговора, ЭтотОбъект.УникальныйИдентификатор);
	
	УстановитьТолькоПросмотр = Ложь;
	ТекущийФайлАктуален = Истина;
	ПодписанЭП = ДанныеФайла.ПодписанЭП;
	ТекстСообщения = "";
	ХранитьВерсии = ДанныеФайла.ХранитьВерсии;
	ФайлРедактируетПользователь = ДанныеФайла.Редактирует;
	
	Если ДанныеФайла.Зашифрован Тогда
		Отказ = Истина;
		УстановитьТолькоПросмотр = Истина;
		Если ЭтоДополнительноеСоглашение Тогда
			ТекстСообщения = НСтр("ru = 'Файл дополнительного соглашения зашифрован. Просмотр текста невозможен.'");
		Иначе
			ТекстСообщения = НСтр("ru = 'Файл договора зашифрован. Просмотр текста договора невозможен.'");
		КонецЕсли;
	// Если договор подписан или нет прав на редактирование, то открываем только на просмотр
	ИначеЕсли ПодписанЭП Или ДоговорПодписан Или Не ЕстьПравоРедактирования() Тогда
		УстановитьТолькоПросмотр = Истина;
	// Если файл редактируется из другого места, то открываем только на просмотр
	ИначеЕсли ДанныеФайла.ФайлРедактируется И НЕ ДанныеФайла.ФайлРедактируетТекущийПользователь Тогда
		УстановитьТолькоПросмотр = Истина;
		Если ЭтоДополнительноеСоглашение Тогда
			ТекстСообщения = СтрШаблон(НСтр("ru = '%1 редактирует текст дополнительного соглашения. Редактирование текста запрещено.'"),
				Строка(ДанныеФайла.Редактирует));
		Иначе
			ТекстСообщения = СтрШаблон(НСтр("ru = '%1 редактирует текст договора. Редактирование текста запрещено.'"),
				Строка(ДанныеФайла.Редактирует));
		КонецЕсли;
	// Если файл редактирует текущий пользователь, то необходимо получить актуальную версию файла
	ИначеЕсли ДанныеФайла.ФайлРедактируетТекущийПользователь Тогда
		ТекущийФайлАктуален = Ложь;
	// Попытаемся заблокировать файл, если не удалось, то установим только просмотр
	Иначе
		Попытка
			ЗаблокироватьДанныеДляРедактирования(ФайлДоговора, , УникальныйИдентификатор);
		Исключение
			УстановитьТолькоПросмотр = Истина;
			Если ЭтоДополнительноеСоглашение Тогда
				ТекстСообщения = НСтр("ru = 'Нельзя отредактировать текст дополнительного соглашения! Файл редактируется другим пользователем!'");
			Иначе
				ТекстСообщения = НСтр("ru = 'Нельзя отредактировать текст договора! Файл договора редактируется другим пользователем!'");
			КонецЕсли;
		КонецПопытки;
	КонецЕсли;
	
	Если УстановитьТолькоПросмотр Тогда
		Если ЗначениеЗаполнено(ТекстСообщения) Тогда
			ОбщегоНазначенияКлиентСервер.СообщитьПользователю(ТекстСообщения);
		КонецЕсли;
		ТолькоПросмотр = Истина;
		Элементы.ФорматированныйДокумент.ТолькоПросмотр = Истина;
		Элементы.ГруппаДобавитьПараметр.Видимость = Ложь;
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ЗагрузитьФайлДоговора()
	
	Попытка
	
		ДанныеФайла   = ПолучитьДанныеФайла(ФайлДоговора, УникальныйИдентификатор, Истина);
		ХранитьВерсии = ДанныеФайла.ХранитьВерсии;
		ФайлРедактируетПользователь = ДанныеФайла.Редактирует;
		
		ДвоичныеДанные = ПолучитьИзВременногоХранилища(ДанныеФайла.СсылкаНаДвоичныеДанныеФайла);
		
		ИмяВременногоФайла = ПолучитьИмяВременногоФайла();
		
		ДвоичныеДанные.Записать(ИмяВременногоФайла);
		
		ТекстовыйДокументHTML = Новый ТекстовыйДокумент;
		ТекстовыйДокументHTML.Прочитать(ИмяВременногоФайла, КодировкаТекста.UTF8);
		
		СтруктураHTML = СтруктураHTMLСКартинками(ТекстовыйДокументHTML.ПолучитьТекст());
		
		ФорматированныйДокумент.УстановитьHTML(СтруктураHTML.ТекстHTML, СтруктураHTML.Вложения);
	
	Исключение
		// Запомним сведения об ошибке для отображения сообщения пользователю при открытии
		СведенияОбОшибке = ИнформацияОбОшибке();
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Произошла ошибка при открытии сохраненного файла: 
					   |%1'"),
			СведенияОбОшибке.Описание);
		
		Если ЭтоДополнительноеСоглашение Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось записать ссылку на файл с текстом дополнительного соглашения 
				|в строке дополнительного соглашения элемента справочника ""Договоры контрагентов"". 
				|Возможно, договор сейчас редактируется другим пользователем. 
				|Укажите этот файл в строке дополнительного соглашения самостоятельно.'");
		Иначе
			ТекстСообщения = НСтр("ru = 'Не удалось записать ссылку на файл с текстом договора 
				|в элементе справочника ""Договоры контрагентов"". 
				|Возможно, договор сейчас редактируется другим пользователем. 
				|Укажите этот файл в форме договора самостоятельно.'");
		КонецЕсли;
		
		ПодробныйТекстОшибки = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Произошла ошибка при открытии сохраненного файла:
					   |%1'"),
			ПодробноеПредставлениеОшибки(СведенияОбОшибке));
		
		ЗаписьЖурналаРегистрации(НСтр("ru = 'Шаблоны договоров. Загрузка текста договора.'", 
			ОбщегоНазначения.КодОсновногоЯзыка()), 
			УровеньЖурналаРегистрации.Ошибка, , , 
			ПодробныйТекстОшибки);
			
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(ТекстСообщения);
		
		ФайлДоговора = Неопределено;
	
	КонецПопытки;
	
	// Удалим временный файл.
	УдалитьВременныйФайлНаСервер(ИмяВременногоФайла);

КонецПроцедуры

&НаКлиенте
Процедура ЗаписатьФайлДоговораНаКлиенте()
	
	ЗаписатьФайлыДоговоровНаСервере(ФорматыСохранения);
	
	// Оповестим основную форму договора о том, что записали новый файл,
	// чтобы установить ссылку на новый файл в договоре.
	Если ФайлДоговора <> ФайлДоговораНовый Тогда
		
		ФайлДоговора = ФайлДоговораНовый;
		
		// По умолчанию считаем, что основной формы договора 
		// в текущем сеансе нет. Если она есть и обработала наше оповещение, 
		// то в ОбработкаОповещения() переменная будет установлена в Истина.
		СозданиеФайлаОбработаноОсновнойФормойДоговора = Ложь;
	
		ПараметрыОповещения = Новый Структура();
		ПараметрыОповещения.Вставить("ФайлДоговора", ФайлДоговора);
		ПараметрыОповещения.Вставить("ДоговорКонтрагента", ДоговорКонтрагента);
		ПараметрыОповещения.Вставить("ЭлектронныйФормат", ЭлектронныйФормат);
		
		Если ЭтоДополнительноеСоглашение Тогда
			Оповестить("ДоговорыКонтрагентов_СозданиеФайлаДополнительногоСоглашения", ПараметрыОповещения, ЭтотОбъект);
		Иначе
			Оповестить("ДоговорыКонтрагентов_СозданиеФайлаДоговора", ПараметрыОповещения, ЭтотОбъект);
		КонецЕсли;
		
		Если Не СозданиеФайлаОбработаноОсновнойФормойДоговора
			И Не ЭтоДополнительноеСоглашение Тогда
			// Текущая форма редактирования текста договора либо открыта из документа,
			// либо основную форму договора уже закрыли, она не получила нашего оповещения
			// и не ответила нам. 
			// Поэтому необходимо самостоятельно записывать ссылку на созданный файл в договор.
			ЗаписатьСсылкаНаФайлВДоговорКонтрагента();
		КонецЕсли; 
		
	КонецЕсли;

КонецПроцедуры

&НаСервере
Процедура ЗаписатьФайлыДоговоровНаСервере(ФорматыСохранения)
	
	// подготовка временной папки
	ИмяВременнойПапки = ФайловаяСистема.СоздатьВременныйКаталог();
	
	Если ЭтоДополнительноеСоглашение Тогда
		РеквизитыСоглашенияСтрокой = ПечатьДоговоровКлиентСервер.РеквизитыДоговораСтрокой(НомерСоглашения, ДатаСоглашения);
		ИмяФайлаБезРасширения = СокрЛП(СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = '%1 Дополнительное соглашение %2'"), Контрагент, РеквизитыСоглашенияСтрокой));
	Иначе
		ИмяФайлаБезРасширения = СокрЛП(СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = '%1 %2'"), Контрагент, ДоговорКонтрагента));
	КонецЕсли;
	ИмяФайлаБезРасширения = ОбщегоНазначенияКлиентСервер.ЗаменитьНедопустимыеСимволыВИмениФайла(ИмяФайлаБезРасширения);
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ДоговорыКонтрагентовПрисоединенныеФайлы.Ссылка КАК ФайлДоговора,
	|	ДоговорыКонтрагентовПрисоединенныеФайлы.Расширение КАК РасширениеФайла
	|ИЗ
	|	Справочник.ДоговорыКонтрагентовПрисоединенныеФайлы КАК ДоговорыКонтрагентовПрисоединенныеФайлы
	|ГДЕ
	|	ДоговорыКонтрагентовПрисоединенныеФайлы.ВладелецФайла = &ВладелецФайла
	|	И ДоговорыКонтрагентовПрисоединенныеФайлы.Наименование = &Наименование";
	
	Запрос.УстановитьПараметр("ВладелецФайла", ДоговорКонтрагента);
	Запрос.УстановитьПараметр("Наименование", ИмяФайлаБезРасширения);
	Выборка = Запрос.Выполнить().Выбрать();
	
	Для Каждого ФорматФайла Из ФорматыСохранения Цикл
	
		Если ФорматФайла = "RTF" Тогда
			
			// Подготовим RTF документ на основе форматированного документа
			ПолноеИмяФайла = ПодготовитьRTFНаОсновеФорматированногоДокумента(ИмяВременнойПапки, ИмяФайлаБезРасширения);
			РасширениеФайла = "rtf";
			
		ИначеЕсли ФорматФайла = "PDF" Тогда
			// Подготовим PDF документ на основе форматированного документа
			ПолноеИмяФайла = ПодготовитьPDFНаОсновеФорматированногоДокумента(ИмяВременнойПапки, ИмяФайлаБезРасширения);
			РасширениеФайла = "pdf";
			
		Иначе
			
			// Подготовим HTML документ на основе форматированного документа.
			ПолноеИмяФайла = ПодготовитьHTMLНаОсновеФорматированногоДокумента(ИмяВременнойПапки, ИмяФайлаБезРасширения);
			РасширениеФайла = "htm";
			
		КонецЕсли;
		ДвоичныеДанные = Новый ДвоичныеДанные(ПолноеИмяФайла);
		АдресФайлаВоВременномХранилище = ПоместитьВоВременноеХранилище(ДвоичныеДанные, УникальныйИдентификатор);
		
		Выборка.Сбросить();
		Если Выборка.НайтиСледующий(РасширениеФайла, "РасширениеФайла") Тогда
			
			РазблокироватьДанныеДляРедактирования(Выборка.ФайлДоговора, УникальныйИдентификатор);
			
			ИнформацияОФайле = Новый Структура();
			ИнформацияОФайле.Вставить("АдресФайлаВоВременномХранилище", АдресФайлаВоВременномХранилище);
			ИнформацияОФайле.Вставить("АдресВременногоХранилищаТекста", "");
			
			РаботаСФайлами.ОбновитьФайл(Выборка.ФайлДоговора, ИнформацияОФайле);
			
			// Если была установлена пометка, то снимем  
			Если ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Выборка.ФайлДоговора, "ПометкаУдаления") Тогда
				СнятьПометкуУдаленияФайлаДоговора(Выборка.ФайлДоговора); 
			КонецЕсли;
			
			ПрисоединенныйФайл = Выборка.ФайлДоговора;
			
		Иначе
			ПараметрыФайла = Новый Структура();
			ПараметрыФайла.Вставить("Автор", Пользователи.ТекущийПользователь());
			ПараметрыФайла.Вставить("ВладелецФайлов", ДоговорКонтрагента);
			ПараметрыФайла.Вставить("ИмяБезРасширения", ИмяФайлаБезРасширения);
			ПараметрыФайла.Вставить("РасширениеБезТочки", РасширениеФайла);
			ПараметрыФайла.Вставить("ВремяИзмененияУниверсальное", Неопределено);
			
			ПрисоединенныйФайл = РаботаСФайлами.ДобавитьФайл(
				ПараметрыФайла, АдресФайлаВоВременномХранилище, "", "");
				
		КонецЕсли;
		
		Если РасширениеФайла = "htm" Или ЭтоДополнительноеСоглашение Тогда
			
			ФайлДоговораНовый = ПрисоединенныйФайл;
			Модифицированность = Ложь;
			
		Иначе
			
			ФайлДоговораНовый = ФайлДоговора;
			Модифицированность = Ложь;

		КонецЕсли;
		
	КонецЦикла;
	
	// Удалим временный файл.
	ФайловаяСистема.УдалитьВременныйКаталог(ИмяВременнойПапки);
	
КонецПроцедуры

&НаСервере
Функция ПодготовитьHTMLНаОсновеФорматированногоДокумента(ИмяПапки, ИмяФайла)
	
	ПолноеИмяФайла = ФайловаяСистема.УникальноеИмяФайла(
		ОбщегоНазначенияКлиентСервер.ДобавитьКонечныйРазделительПути(ИмяПапки) + ИмяФайла);
	
	Если Не ОбщегоНазначенияБП.ЭтоИмяВременногоФайла(ПолноеИмяФайла) Тогда
		ВызватьИсключение "Работать с HTML-файлом можно только во временном каталоге";
	КонецЕсли;
	
	ТекстHTML = "";
	Вложения = Новый Структура();
	
	ФорматированныйДокумент.ПолучитьHTML(ТекстHTML, Вложения);
	
	ТекстовыйДокументHTML = Новый ТекстовыйДокумент;
	ТекстовыйДокументHTML.УстановитьТекст(ТекстHTML);
	// записываем документ во временную папку
	ТекстовыйДокументHTML.Записать(ПолноеИмяФайла, КодировкаТекста.UTF8);
	
	// Подготовим картинки
	ИмяПапкиКартинок = "_files";
	ПутьКПапкеКартинок = ОбщегоНазначенияКлиентСервер.ДобавитьКонечныйРазделительПути(ИмяПапки) + "_files";
	СоздатьКаталог(ПутьКПапкеКартинок);
	Для Каждого Картинка Из Вложения Цикл
		
		Картинка.Значение.Записать(ПутьКПапкеКартинок+ "\" + Картинка.Ключ + ".jpg");
		
	КонецЦикла;
	
	ВставитьКартинкиВHTML(ПолноеИмяФайла);
	
	УдалитьВременныйФайлНаСервер(ПутьКПапкеКартинок);
	
	Возврат ПолноеИмяФайла;
	
КонецФункции

&НаСервере
Функция ПодготовитьRTFНаОсновеФорматированногоДокумента(ИмяПапки, ИмяФайла)
	
	Если ЭлектронныйФормат Тогда
		ФорматированныйДокументДляСохранения = ФорматированныйДокументБезСлужебныхМеток();
	Иначе
		ФорматированныйДокументДляСохранения = ФорматированныйДокумент;
	КонецЕсли;
	
	ПолноеИмяФайла = ФайловаяСистема.УникальноеИмяФайла(
		ОбщегоНазначенияКлиентСервер.ДобавитьКонечныйРазделительПути(ИмяПапки) + ИмяФайла);
	
	ЭлектронноеВзаимодействиеБП.КонвертироватьВФорматRTF(ФорматированныйДокументДляСохранения, ПолноеИмяФайла);
	
	Возврат ПолноеИмяФайла;
	
КонецФункции

&НаСервере
Функция ПодготовитьPDFНаОсновеФорматированногоДокумента(ИмяПапки, ИмяФайла)
	
	Если ЭлектронныйФормат Тогда
		ФорматированныйДокументДляСохранения = ФорматированныйДокументБезСлужебныхМеток();
	Иначе
		ФорматированныйДокументДляСохранения = ФорматированныйДокумент;
	КонецЕсли;
	
	ПолноеИмяФайла = ФайловаяСистема.УникальноеИмяФайла(
		ОбщегоНазначенияКлиентСервер.ДобавитьКонечныйРазделительПути(ИмяПапки) + ИмяФайла);
	
	ТекстHTML = "";
	Вложения = Новый Структура();
	
	ФорматированныйДокументДляСохранения.ПолучитьHTML(ТекстHTML, Вложения);
	
	// Т.к. PDF является нередактируемым, то чтобы при печати не было избыточных выделений,
	// снимем желтый цвет с параметров шаблона, который раньше сами устанавливали.
	// Форматирование, сделанное пользователем, не меняем.
	// Выполняем это во вспомогательном форматированном документе.
	ВспомогательныйФорматированныйДокумент = Новый ФорматированныйДокумент();
	ВспомогательныйФорматированныйДокумент.УстановитьHTML(ТекстHTML, Вложения);
	
	ЦветФонаПоУмолчанию = ПечатьДоговоровКлиентСервер.ЦветФонаПоУмолчанию();
	
	Для каждого Параграф Из ВспомогательныйФорматированныйДокумент.Элементы Цикл
	
		Для каждого ЭлементДокумента Из Параграф.Элементы Цикл
		
			Если ТипЗнч(ЭлементДокумента) <> Тип("ТекстФорматированногоДокумента") Тогда
				Продолжить;
			КонецЕсли;
			
			Если ЭлементДокумента.ЦветФона = ЦветФонаЗаменяемогоПараметра Тогда
				ЭлементДокумента.ЦветФона = ЦветФонаПоУмолчанию;
			КонецЕсли;
		
		КонецЦикла;
	
	КонецЦикла;
	
	ВспомогательныйФорматированныйДокумент.Записать(ПолноеИмяФайла, ТипФайлаФорматированногоДокумента.PDF);
	
	Возврат ПолноеИмяФайла;
	
КонецФункции

&НаСервере
Процедура ЗаписатьСсылкаНаФайлВДоговорКонтрагента()
	
	ДоговорДляЗаписи = РеквизитФормыВЗначение("ДоговорОбъект");
	Попытка

		ДоговорДляЗаписи.Заблокировать();
		ДоговорДляЗаписи.ФайлДоговора = ФайлДоговора;
		ДоговорДляЗаписи.ЭлектронныйФормат = ЭлектронныйФормат;
		ДоговорДляЗаписи.Записать();
		ДоговорДляЗаписи.Разблокировать();
		
	Исключение
		СведенияОбОшибке = ИнформацияОбОшибке();
		ТекстСообщения = НСтр("ru = 'Не удалось записать ссылку на файл с текстом договора 
									|в элементе справочника ""Договоры контрагентов"". 
									|Возможно, договор сейчас редактируется другим пользователем.'");
	
		ПодробныйТекстОшибки = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Не удалось записать ссылку на файл с текстом договора в элементе справочника ""Договоры контрагентов"" по причине: %1'"),
			ПодробноеПредставлениеОшибки(СведенияОбОшибке));
		
		ЗаписьЖурналаРегистрации(НСтр("ru = 'Шаблоны договоров. Сохранение текста договора.'", 
			ОбщегоНазначения.КодОсновногоЯзыка()), 
			УровеньЖурналаРегистрации.Ошибка, , , 
			ПодробныйТекстОшибки);
	
		ВызватьИсключение ТекстСообщения;
			
	КонецПопытки;

КонецПроцедуры

&НаСервере
Процедура СнятьПометкуУдаленияФайлаДоговора(ОбновленныйФайлДоговора)
	
	ФайлДоговораДляЗаписи = ОбновленныйФайлДоговора.ПолучитьОбъект();
	Попытка

		ФайлДоговораДляЗаписи.Заблокировать();
		ФайлДоговораДляЗаписи.ПометкаУдаления = Ложь;
		ФайлДоговораДляЗаписи.Записать();
		ФайлДоговораДляЗаписи.Разблокировать();
		
	Исключение
		СведенияОбОшибке = ИнформацияОбОшибке();
		Если ЭтоДополнительноеСоглашение Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось записать файл с текстом дополнительного соглашения 
										|в элементе справочника ""Договоры контрагентов"". 
										|Возможно, файл сейчас редактируется другим пользователем.'");
			
			ПодробныйТекстОшибки = НСтр("ru = 'Не удалось записать файл с текстом дополнительного соглашения в элементе справочника ""Договоры контрагентов"" по причине: %1'");
		Иначе
			ТекстСообщения = НСтр("ru = 'Не удалось записать файл с текстом договора 
										|в элементе справочника ""Договоры контрагентов"". 
										|Возможно, файл сейчас редактируется другим пользователем.'");
			
			ПодробныйТекстОшибки = НСтр("ru = 'Не удалось записать файл с текстом договора в элементе справочника ""Договоры контрагентов"" по причине: %1'");
		КонецЕсли;
	
		ПодробныйТекстОшибки = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			ПодробныйТекстОшибки,
			ПодробноеПредставлениеОшибки(СведенияОбОшибке));
		
		ЗаписьЖурналаРегистрации(НСтр("ru = 'Шаблоны договоров. Сохранение текста договора.'", 
			ОбщегоНазначения.КодОсновногоЯзыка()), 
			УровеньЖурналаРегистрации.Ошибка, , , 
			ПодробныйТекстОшибки);
	
		ВызватьИсключение ТекстСообщения;
			
	КонецПопытки;

КонецПроцедуры

&НаСервере
Процедура РазблокироватьФайлНаСервере()
	
	РазблокироватьДанныеДляРедактирования(ФайлДоговора, УникальныйИдентификатор);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьДанныеФайла(Знач ПрисоединенныйФайл, Знач ИдентификаторФормы = Неопределено, Знач ПолучатьСсылкуНаДвоичныеДанные = Истина)
	
	Возврат РаботаСФайлами.ДанныеФайла(
		ПрисоединенныйФайл, ИдентификаторФормы, ПолучатьСсылкуНаДвоичныеДанные);
	
КонецФункции

&НаКлиенте
Процедура ЗакончитьРедактированиеВыполненоПомещение(Результат, ПараметрыФайла) Экспорт
	
	Если Результат.Свойство("ОписаниеОшибки") Тогда
		Если ЭтоДополнительноеСоглашение Тогда
			ТекстСообщения = НСтр("ru = 'Нельзя отредактировать текст дополнительного соглашения, т.к. файл уже редактируется!
			|Отмените редактирования файла и откройте договор еще раз.'");
		Иначе
			ТекстСообщения = НСтр("ru = 'Нельзя отредактировать текст договора, т.к. файл уже редактируется!
			|Отмените редактирования файла и откройте договор еще раз.'");
		КонецЕсли;
		
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(ТекстСообщения);
		ТолькоПросмотр = Истина;
		Элементы.ФорматированныйДокумент.ТолькоПросмотр = Истина;
	КонецЕсли;
	
	ЗагрузитьФайлДоговораПослеПомещения();
	
КонецПроцедуры

&НаСервере
Процедура ЗагрузитьФайлДоговораПослеПомещения()
	
	// Загрузим новый файл договора, после того как освободили
	ЗагрузитьФайлДоговора();
	УправлениеФормой();
	
КонецПроцедуры

&НаСервере
Процедура УдалитьВременныйФайлНаСервер(ИмяФайла)

	Если НЕ ЗначениеЗаполнено(ИмяФайла) Тогда
		Возврат;
	КонецЕсли;

	// Удалим временный файл.
	Попытка
		УдалитьФайлы(ИмяФайла);
	Исключение
		СведенияОбОшибке = ИнформацияОбОшибке();
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Не удалось удалить временный файл по причине: %1'"),
			ПодробноеПредставлениеОшибки(СведенияОбОшибке));
		
		ЗаписьЖурналаРегистрации(НСтр("ru = 'Шаблоны договоров. Сохранение текста договора.'", 
			ОбщегоНазначения.КодОсновногоЯзыка()), 
			УровеньЖурналаРегистрации.Ошибка, , , 
			ТекстСообщения);
	КонецПопытки;

КонецПроцедуры

&НаСервере
Функция ПоместитьФайлыВоВременноеХранилищеНаСервере(ФорматыСохранения)
	
	Папка = ПолучитьИмяВременногоФайла();
	СоздатьКаталог(Папка);
	
	// Указываем имя файла вложения
	ИмяДоговора = ИмяДоговора();
	ИмяДоговора = ОбщегоНазначенияКлиентСервер.ЗаменитьНедопустимыеСимволыВИмениФайла(ИмяДоговора);
	
	МассивДанных = Новый Массив;
	Для Каждого ФорматФайла Из ФорматыСохранения Цикл
		
		Если ФорматФайла = "RTF" Тогда
			
			ИмяФайла = ИмяДоговора + ".rtf";
			
			// Подготовим RTF документ на основе форматированного документа
			ПолноеИмяФайла = ПодготовитьRTFНаОсновеФорматированногоДокумента(Папка, ИмяФайла);

		ИначеЕсли ФорматФайла = "PDF" Тогда
		
			ИмяФайла = ИмяДоговора + ".pdf";
		
			// Подготовим PDF документ на основе форматированного документа
			ПолноеИмяФайла = ПодготовитьPDFНаОсновеФорматированногоДокумента(Папка, ИмяФайла);
			
		Иначе
			
			ИмяФайла = ИмяДоговора + ".htm";
			
			// Подготовим HTML документ на основе форматированного документа
			ПолноеИмяФайла = ПодготовитьHTMLНаОсновеФорматированногоДокумента(Папка, ИмяФайла);
			
		КонецЕсли;
		ДвоичныеДанные = Новый ДвоичныеДанные(ПолноеИмяФайла);
		ПутьВоВременномХранилище = ПоместитьВоВременноеХранилище(ДвоичныеДанные, ЭтотОбъект.УникальныйИдентификатор);
		ОписаниеФайла = Новый Структура;
		ОписаниеФайла.Вставить("Представление", ИмяФайла);
		ОписаниеФайла.Вставить("АдресВоВременномХранилище", ПутьВоВременномХранилище);
		
		МассивДанных.Добавить(ОписаниеФайла);
		
	КонецЦикла;
	УдалитьВременныйФайлНаСервер(Папка);
	
	Возврат МассивДанных;

КонецФункции

&НаКлиенте
Процедура СохранитьДоговорВПапку(Знач ПапкаДляСохранения)
	
	МассивДанных = ПоместитьФайлыВоВременноеХранилищеНаСервере(ФорматыСохранения);
	
	#Если ВебКлиент Тогда
		Для Каждого Элемент Из МассивДанных Цикл
			
			ПолучитьФайл(Элемент.АдресВоВременномХранилище, Элемент.Представление, Истина);
			
		КонецЦикла;
	#Иначе
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ПапкаДляСохранения", ПапкаДляСохранения);
		
		Если МобильныйКлиентБПКлиент.ЭтоМобильныйКлиент() Тогда
			ОписаниеОповещения = Новый ОписаниеОповещения("ОтправитьФайлыЧерезПоделитьсяПослеПолученияФайлов", МобильныйКлиентБПКлиент);
		Иначе
			ОписаниеОповещения = Новый ОписаниеОповещения("СохранитьДоговорВПапкуЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		КонецЕсли;
		
		
		ПапкаДляСохранения = ОбщегоНазначенияКлиентСервер.ДобавитьКонечныйРазделительПути(ПапкаДляСохранения);
		
		ПолучаемыеФайлы = Новый Массив;
		Для Каждого Элемент Из МассивДанных Цикл
		
			ПолучаемыйФайл 			= Новый ОписаниеПередаваемогоФайла;
			ПолучаемыйФайл.Имя 		= Элемент.Представление;
			ПолучаемыйФайл.Хранение = Элемент.АдресВоВременномХранилище;
			
			ПолучаемыеФайлы.Добавить(ПолучаемыйФайл);
		
		КонецЦикла;
		
		НачатьПолучениеФайлов(ОписаниеОповещения, ПолучаемыеФайлы, ПапкаДляСохранения, Ложь);
		
	#КонецЕсли
	
КонецПроцедуры

&НаКлиенте
Процедура СохранитьДоговорВПапкуЗавершение(ПолученныеФайлы, ДополнительныеПараметры) Экспорт

	Если ПолученныеФайлы = Неопределено Тогда
		ПоказатьПредупреждение(, НСтр("ru = 'При сохранении возникла ошибка'"));
	Иначе
		Состояние(НСтр("ru = 'Сохранение успешно завершено'"), , НСтр("ru = 'в папку:'") + " " + ДополнительныеПараметры.ПапкаДляСохранения);
	КонецЕсли;

КонецПроцедуры

&НаСервереБезКонтекста
Функция ЕстьПравоРедактирования()
	Возврат ПравоДоступа("Редактирование", Метаданные.Справочники.ДоговорыКонтрагентов);
КонецФункции

&НаСервере
Функция ПолучитьФайлДополнительногоСоглашенияДляЭДО(ФорматОтправки)
	
	ИмяВременнойПапки = ФайловаяСистема.СоздатьВременныйКаталог();
	
	ИмяФайлаБезРасширения = ИмяДоговора();
	ИмяФайлаБезРасширения = ОбщегоНазначенияКлиентСервер.ЗаменитьНедопустимыеСимволыВИмениФайла(ИмяФайлаБезРасширения);
	
	Если ФорматОтправки = "RTF" Тогда
		ПолноеИмяФайла = ПодготовитьRTFНаОсновеФорматированногоДокумента(ИмяВременнойПапки, ИмяФайлаБезРасширения);
		РасширениеФайла = "rtf";
		
	ИначеЕсли ФорматОтправки = "PDF" Тогда
		ПолноеИмяФайла = ПодготовитьPDFНаОсновеФорматированногоДокумента(ИмяВременнойПапки, ИмяФайлаБезРасширения);
		РасширениеФайла = "pdf";
		
	Иначе
		ПолноеИмяФайла = ПодготовитьHTMLНаОсновеФорматированногоДокумента(ИмяВременнойПапки, ИмяФайлаБезРасширения);
		РасширениеФайла = "htm";
		
	Конецесли;
	
	ДвоичныеДанные = Новый ДвоичныеДанные(ПолноеИмяФайла);
	АдресФайлаВоВременномХранилище = ПоместитьВоВременноеХранилище(ДвоичныеДанные, УникальныйИдентификатор);
	
	// Удалим временный файл.
	ФайловаяСистема.УдалитьВременныйКаталог(ИмяВременнойПапки);
	
	ДанныеФайла = Новый Структура;
	ДанныеФайла.Вставить("СсылкаНаДвоичныеДанныеФайла", АдресФайлаВоВременномХранилище);
	ДанныеФайла.Вставить("Наименование", ИмяФайлаБезРасширения);
	ДанныеФайла.Вставить("Расширение", РасширениеФайла);
	
	Возврат ДанныеФайла;
	
КонецФункции

#КонецОбласти

#Область ОтправкаПоЭлектроннойПочте

&НаКлиенте
Процедура ОтправитьЗавершение(РезультатЗакрытия, ДополнительныеПараметры) Экспорт
	
	Если РезультатЗакрытия = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если МобильныйКлиентБПКлиент.ЭтоМобильныйКлиент()
		И Не ДоступныУчетныеЗаписиДляОтправкиМК Тогда
		ОтправитьДоговорПоПочтеНастройкаУчетнойЗаписиПредложена(Истина, РезультатЗакрытия);
	Иначе
		// Перед созданием нового письма проверим настройки учетной записи для отправки почты
		ОписаниеОповещения = Новый ОписаниеОповещения("ОтправитьДоговорПоПочтеНастройкаУчетнойЗаписиПредложена", ЭтотОбъект, РезультатЗакрытия);
		РаботаСПочтовымиСообщениямиКлиент.ПроверитьНаличиеУчетнойЗаписиДляОтправкиПочты(ОписаниеОповещения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправитьДоговорПоПочтеНастройкаУчетнойЗаписиПредложена(УчетнаяЗаписьНастроена, ДополнительныеПараметры) Экспорт
	
	// Если нет настроенной учетной записи прерываем подготовку отправки
	Если УчетнаяЗаписьНастроена <> Истина Тогда
		Возврат;
	КонецЕсли;

	ПараметрыОтправкиПисьма = ДополнительныеПараметры;
	
	ПодключитьОбработчикОжидания("Подключаемый_ОткрытьПисьмоСВложениями", 0.1, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОткрытьПисьмоСВложениями()

	// Подготавливаем параметры нового письма
	ПараметрыНовогоПисьма = ПодготовитьПараметрыПисьма(ПараметрыОтправкиПисьма);
	
	Если МобильныйКлиентБПКлиент.ДоступенМобильныйПочтовыйКлиент()
		И Не ДоступныУчетныеЗаписиДляОтправкиМК Тогда
		ОтправкаПочтовыхСообщенийКлиент.ОтправитьНовоеПисьмоМК(ПараметрыНовогоПисьма);
	Иначе
		// Инициируем создание нового исходящего письма
		РаботаСПочтовымиСообщениямиКлиент.СоздатьНовоеПисьмо(ПараметрыНовогоПисьма);
	КонецЕсли;

КонецПроцедуры // Подключаемый_ОткрытьПисьмоСВложениями()

&НаСервере
Функция УдалитьЛишниеПробелы(ИсходнаяСтрока)

	Пока СтрНайти(ИсходнаяСтрока, "  ") > 0  Цикл
	
		ИсходнаяСтрока = СтрЗаменить(ИсходнаяСтрока, "  ", " ");
	
	КонецЦикла; 
	
	Возврат ИсходнаяСтрока;

КонецФункции

&НаСервере
Функция ПодготовитьПараметрыПисьма(ПараметрыОтправки)

	Организация = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ДоговорКонтрагента, "Организация");
	
	// Сформируем вложения, в письмо они вставляются в виде массива
	// Поэтоиму подготовим массив с единственным значением - файлом договора
	СписокВложений = СписокВложений(ПараметрыОтправки);
	
	ИмяДоговора = ИмяДоговора();
	
	НаименованиеОрганизации = ОтправкаПочтовыхСообщений.НаименованиеКонтрагента(Организация);
	НаименованиеКонтрагента = ОтправкаПочтовыхСообщений.НаименованиеКонтрагента(Контрагент);
	
	ТемаПисьма = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = '%1 для %2 от %3'"),
		ИмяДоговора,
		НаименованиеКонтрагента,
		НаименованиеОрганизации);
	
	Если ЭтоДополнительноеСоглашение Тогда
		ТекстПисьма = НСтр("ru = 'К письму приложено %1 для %2 от %3'");
	Иначе
		ТекстПисьма = НСтр("ru = 'К письму приложен %1 для %2 от %3'");
	КонецЕсли;
	ТекстПисьма = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(ТекстПисьма,
		ИмяДоговора,
		НаименованиеКонтрагента,
		НаименованиеОрганизации);
	
	ПараметрыНовогоПисьма = Новый Структура;
	ПараметрыНовогоПисьма.Вставить("Получатель", ОтправкаПочтовыхСообщений.АдресаЭлектроннойПочты(Контрагент));
	ПараметрыНовогоПисьма.Вставить("Тема", УдалитьЛишниеПробелы(ТемаПисьма));
	ПараметрыНовогоПисьма.Вставить("Текст", УдалитьЛишниеПробелы(ТекстПисьма));
	ПараметрыНовогоПисьма.Вставить("Вложения", СписокВложений);
	ПараметрыНовогоПисьма.Вставить("УдалятьФайлыПослеОтправки", Истина);
	
	Возврат ПараметрыНовогоПисьма
	
КонецФункции

&НаСервере
Функция СписокВложений(ПараметрыОтправки)
	
	Результат = Новый Массив;
	
	// Подготовка временной папки.
	ИмяВременнойПапки = ПолучитьИмяВременногоФайла();
	СоздатьКаталог(ИмяВременнойПапки);
	
	// Указываем имя файла вложения.
	ИмяДоговора = ИмяДоговора();
	ИмяДоговора = ОбщегоНазначенияКлиентСервер.ЗаменитьНедопустимыеСимволыВИмениФайла(ИмяДоговора);
	
	ИменаВложений = Новый Массив;
	
	Для каждого ФорматСохранения Из ПараметрыОтправки.ФорматыСохранения Цикл
	
		// Отправка текста самого договора.
		Если ПараметрыОтправки.ОбъектыОтправки.Найти("Договор") <> Неопределено Тогда
	
			Если ФорматСохранения = "RTF" Тогда
				ИмяФайла = ИмяДоговора + ".rtf";
				ПолноеИмяФайла = ПодготовитьRTFНаОсновеФорматированногоДокумента(ИмяВременнойПапки, ИмяФайла);
			
			ИначеЕсли ФорматСохранения = "PDF" Тогда
				ИмяФайла = ИмяДоговора + ".pdf";
				ПолноеИмяФайла = ПодготовитьPDFНаОсновеФорматированногоДокумента(ИмяВременнойПапки, ИмяФайла);

			Иначе
				ИмяФайла = ИмяДоговора + ".htm";
				ПолноеИмяФайла = ПодготовитьHTMLНаОсновеФорматированногоДокумента(ИмяВременнойПапки, ИмяФайла);
			
			Конецесли;
			
			// Добавляем подготовленный документ в список вложений
			Если ПараметрыОтправки.УпаковатьВАрхив Тогда
				ИменаВложений.Добавить(ПолноеИмяФайла);
			Иначе
				ДобавитьОписаниеФайлаВложения(Результат, ИмяФайла, ПолноеИмяФайла);
			КонецЕсли;
			
		КонецЕсли;
		
		// Если печатаем из счета на оплату, то могут добавить во вложении "Приложение к договору".
		Если ПараметрыОтправки.ОбъектыОтправки.Найти("ПриложениеКДоговору") <> Неопределено Тогда
			
			МассивОбъектов = Новый Массив;
			МассивОбъектов.Добавить(ОбъектПечати);
			
			ТаблицаСведенийСчетНаОплату = Документы.СчетНаОплатуПокупателю.ПолучитьТаблицуСведенийСчетаНаОплату(МассивОбъектов);
			ТабличныйДокументПриложенияКДоговору = ПечатьТорговыхДокументов.ПечатьПриложенияКДоговору(ТаблицаСведенийСчетНаОплату, Новый СписокЗначений, Новый Структура);
			
			// Указываем имя файла вложения.
			ИмяДоговораДляПриложения = СокрЛП(СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Договору № %1 от %2'"), НомерДоговора, Формат(ДатаДоговора, "ДЛФ=DD")));
			ИмяДоговораДляПриложения = ОбщегоНазначенияКлиентСервер.ЗаменитьНедопустимыеСимволыВИмениФайла(ИмяДоговораДляПриложения);

			ИмяФайла = НСтр("ru = 'Приложение к '") + ИмяДоговораДляПриложения;

			Если ФорматСохранения = "RTF" Тогда
				ИмяФайла = ИмяФайла + ".htm"; // Табличный документ в rtf не сохраняется, оставляем в виде HTML.
				ТипФайла = ТипФайлаТабличногоДокумента.HTML;
			ИначеЕсли ФорматСохранения = "PDF" Тогда
				ИмяФайла = ИмяФайла + ".pdf";
				ТипФайла = ТипФайлаТабличногоДокумента.PDF;
			Иначе
				ИмяФайла = ИмяФайла + ".htm";
				ТипФайла = ТипФайлаТабличногоДокумента.HTML;
			КонецЕсли;
			
			ПолноеИмяФайла = ФайловаяСистема.УникальноеИмяФайла(
				ОбщегоНазначенияКлиентСервер.ДобавитьКонечныйРазделительПути(ИмяВременнойПапки) + ИмяФайла);
			ТабличныйДокументПриложенияКДоговору.Записать(ПолноеИмяФайла, ТипФайла);
			
			// Добавляем подготовленный документ в список вложений.
			Если ПараметрыОтправки.УпаковатьВАрхив Тогда
				ИменаВложений.Добавить(ПолноеИмяФайла);
			Иначе
				ДобавитьОписаниеФайлаВложения(Результат, ИмяФайла, ПолноеИмяФайла);
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЦикла;

	// Упакуем все файлы в ZIP-архив при необходимости.
	Если ПараметрыОтправки.УпаковатьВАрхив Тогда
		ИмяZipФайла = ФайловаяСистема.УникальноеИмяФайла(
			ОбщегоНазначенияКлиентСервер.ДобавитьКонечныйРазделительПути(ИмяВременнойПапки) + ИмяДоговора + ".zip");
		ЗаписьZIP = Новый ЗаписьZIPФайла(ИмяZipФайла);
		
		Для каждого ИмяВложения Из ИменаВложений Цикл
			ЗаписьZIP.Добавить(ИмяВложения);
		КонецЦикла;
		
		ЗаписьZIP.Записать();

		ДобавитьОписаниеФайлаВложения(Результат, ИмяДоговора + ".zip", ИмяZipФайла);
	КонецЕсли;

	// Удаляем временные файлы
	УдалитьВременныйФайлНаСервер(ИмяВременнойПапки);

	Возврат Результат;
	
КонецФункции

&НаСервере
Процедура ДобавитьОписаниеФайлаВложения(Вложения, ИмяФайла, ПолноеИмяФайла)
	
	// Проверим, что полное имя файла - во временной папке.
	// Т.к. эта процедура вызывается только для файлов, которые программно помещаются во
	// временный каталог. Если файл не во временном каталоге - это ошибочная ситуация, такого быть не может,
	// нужно вызвать исключение.
	
	Если Не ОбщегоНазначенияБП.ЭтоИмяВременногоФайла(ПолноеИмяФайла) Тогда
		ВызватьИсключение "Добавлять описание файла вложения можно только из временного каталога";
	КонецЕсли;
	
	ДвоичныеДанные = Новый ДвоичныеДанные(ПолноеИмяФайла);
	ПутьВоВременномХранилище = ПоместитьВоВременноеХранилище(ДвоичныеДанные, УникальныйИдентификатор);

	ОписаниеФайла = Новый Структура;
	ОписаниеФайла.Вставить("Представление", ИмяФайла);
	ОписаниеФайла.Вставить("АдресВоВременномХранилище", ПутьВоВременномХранилище);
	
	Вложения.Добавить(ОписаниеФайла);

КонецПроцедуры

&НаСервере
Функция СтруктураHTMLСКартинками(ТекстHTML)
	
	СтруктураHTML = Новый Структура();
	
	Вложения = Новый Структура();
	ТекстСДвоичнымиДанными = ТекстHTML;
	
	КоличествоВхождений = СтрЧислоВхождений(ТекстHTML, "data:image/");
	
	Для Сч = 1 По КоличествоВхождений Цикл
		// НачалоКартинкаВДвоичныхДанных - здесь начинается описание картинки в двоичных данных
		НачалоКартинкаВДвоичныхДанных = СтрНайти(ТекстСДвоичнымиДанными, "data:image/");
		// НачалоДвоичныхДанных - здесь начинаются закодированные двоичные данные
		НачалоДвоичныхДанных = СтрНайти(ТекстСДвоичнымиДанными, ";base64,") + 8;
		// КартинкаВДвоичныхДанных - Текст двоичных данных, который нужно заменить на картинку в виде пути
		КартинкаВДвоичныхДанных = Сред(ТекстСДвоичнымиДанными, НачалоКартинкаВДвоичныхДанных, НачалоДвоичныхДанных-НачалоКартинкаВДвоичныхДанных);
		// Отрезаем использованную подстроку, чтобы повторно в ней не искать
		ТекстСДвоичнымиДанными = Сред(ТекстСДвоичнымиДанными, НачалоДвоичныхДанных);
		// КонецДвоичныхДанных - здесь заканчиваются закодированные двоичные данные
		КонецДвоичныхДанных = СтрНайти(ТекстСДвоичнымиДанными, "='");
		Если КонецДвоичныхДанных = 0 Тогда
			КонецДвоичныхДанных = СтрНайти(ТекстСДвоичнымиДанными, "=""");
		КонецЕсли;
		// Непосредственно двоичные данные
		ДвоичныеДанные = Лев(ТекстСДвоичнымиДанными, КонецДвоичныхДанных);
		// Вставляем в виде вложения картинку
		Вложения.Вставить("image"+сч, Новый Картинка(Base64Значение(ДвоичныеДанные)));
		// Собрали текст для замены Картинки в двоичных данных, на картинку в виде вложения
		КартинкаВДвоичныхДанных = КартинкаВДвоичныхДанных + ДвоичныеДанные;
		// Отрезаем обработанную часть текста
		ТекстСДвоичнымиДанными = Сред(ТекстСДвоичнымиДанными, КонецДвоичныхДанных);
		// Меняем в тексте картинку в двоичных данных, на вложение.
		ТекстHTML = СтрЗаменить(ТекстHTML, КартинкаВДвоичныхДанных, "image"+сч);
	КонецЦикла;
	
	СтруктураHTML.Вставить("ТекстHTML", ТекстHTML);
	СтруктураHTML.Вставить("Вложения", Вложения);
	
	Возврат СтруктураHTML;
	
КонецФункции

&НаСервере
Процедура ВставитьКартинкиВHTML(ИмяФайлаHTML)
	
	Если Не ОбщегоНазначенияБП.ЭтоИмяВременногоФайла(ИмяФайлаHTML) Тогда
		ВызватьИсключение "Работать с HTML-файлом можно только во временном каталоге";
	КонецЕсли;
	
	ТекстовыйДокумент = Новый ТекстовыйДокумент();
	ТекстовыйДокумент.Прочитать(ИмяФайлаHTML, КодировкаТекста.UTF8);
	ТекстHTML = ТекстовыйДокумент.ПолучитьТекст();
	
	ФайлHTML = Новый Файл(ИмяФайлаHTML);
	
	ИмяПапкиКартинок = "_files";
	ПутьКПапкеКартинок = СтрЗаменить(ФайлHTML.ПолноеИмя, ФайлHTML.Имя, ИмяПапкиКартинок);
	
	// ожидается, что в папке будут только картинки
	ФайлыКартинок = НайтиФайлы(ПутьКПапкеКартинок, "*");
	
	Для Каждого ФайлКартинки Из ФайлыКартинок Цикл
		КартинкаТекстом = Base64Строка(Новый ДвоичныеДанные(ФайлКартинки.ПолноеИмя));
		КартинкаТекстом = "data:image/" + Сред(ФайлКартинки.Расширение,2) + ";base64," + Символы.ПС + КартинкаТекстом;
		
		ТекстHTML = СтрЗаменить(ТекстHTML, ФайлКартинки.ИмяБезРасширения, КартинкаТекстом);
	КонецЦикла;
		
	ТекстовыйДокумент.УстановитьТекст(ТекстHTML);
	ТекстовыйДокумент.Записать(ИмяФайлаHTML, КодировкаТекста.UTF8);
	
КонецПроцедуры

&НаКлиенте
Процедура СохранитьТекстДоговораВФайлДоговора()
	
	ФорматыСохранения = Новый Массив;
	ФорматыСохранения.Добавить(ТипФайлаТабличногоДокумента.HTML);
	ЗаписатьФайлДоговораНаКлиенте();

КонецПроцедуры

&НаСервере
Функция ФорматированныйДокументБезСлужебныхМеток()
	
	ТекстHTML = "";
	Вложения = Новый Структура();
	
	ФорматированныйДокумент.ПолучитьHTML(ТекстHTML, Вложения);
	
	ФорматированныйДокументБезМеток = Новый ФорматированныйДокумент();
	ФорматированныйДокументБезМеток.УстановитьHTML(ТекстHTML, Вложения);
	
	ЗакладкаКонцаДокумента = ФорматированныйДокументБезМеток.ПолучитьЗакладкуКонца();
				
	МассивЭлементов = ФорматированныйДокументБезМеток.СформироватьЭлементы(, ЗакладкаКонцаДокумента);
					
	Сч = 0;

	Пока Сч < МассивЭлементов.Количество() Цикл
		
		ЭлементДокумента = МассивЭлементов[Сч];
	
		Если ТипЗнч(ЭлементДокумента) <> Тип("ТекстФорматированногоДокумента") Тогда
			Сч = Сч + 1;
			Продолжить;
		КонецЕсли;

		Если СтрНайти(ЭлементДокумента.Текст, "[#") > 0 Или СтрНайти(ЭлементДокумента.Текст, "#]") > 0 Тогда
			ФорматированныйДокументБезМеток.Удалить(ЭлементДокумента.ЗакладкаНачала, ЭлементДокумента.ЗакладкаКонца);
		КонецЕсли;
		
		Сч = Сч + 1; 
		
	КонецЦикла;
	
	Возврат ФорматированныйДокументБезМеток;
	
КонецФункции

&НаСервере
Функция ДокументЭлектронныйФорматДляПечати() 
	
	Возврат ПоместитьВоВременноеХранилище(ФорматированныйДокументБезСлужебныхМеток(), УникальныйИдентификатор);
	
КонецФункции

&НаКлиенте
Процедура ПечатьДоговораЭлектронныйФормат(Команда)  
	
	АдресДокумента = ДокументЭлектронныйФорматДляПечати(); 
	ПараметрыФормы = Новый Структура;
	ПараметрыФормы.Вставить("АдресДокумента", АдресДокумента);
	ПараметрыФормы.Вставить("ОбъектПечати", ДоговорКонтрагента);
	ПараметрыФормы.Вставить("Контрагент", Контрагент);

	ОткрытьФорму("Справочник.ДоговорыКонтрагентов.Форма.ФормаПечатиЭлектронногоФормата", ПараметрыФормы, ЭтотОбъект);
	
КонецПроцедуры

#КонецОбласти

// ЭлектронноеВзаимодействие.ОбменСКонтрагентами

&НаКлиенте
Процедура Подключаемый_ВыполнитьКомандуЭДО(Команда)
	
	ЕстьРабочийЭДД = ПечатьДоговоровВызовСервера.ЕстьЭлектронныйДоговорныйДокумент(ДоговорКонтрагента);
	
	ЯвляетсяКомандойФормированияЭД = СтрНайти(Команда.Имя, "СформироватьПодписатьОтправитьЭД") <> 0 Или
		СтрНайти(Команда.Имя, "ОткрытьАктуальныйЭД") <> 0;
		
	// Для ограниченных прав	
	Если Не ЕстьПравоРедактирования() Тогда
		
		Если ЕстьРабочийЭДД Или Не ЯвляетсяКомандойФормированияЭД Тогда
			ЭлектронноеВзаимодействиеКлиент.ВыполнитьПодключаемуюКомандуЭДО(Команда, ЭтотОбъект, ДоговорОбъект);
		ИначеЕсли Не ЕстьРабочийЭДД Тогда
			ОбщегоНазначенияКлиент.СообщитьПользователю(НСтр("ru = 'Нет прав для формирования электронного документа.'"));
		КонецЕсли;
				
		Возврат;
		
	КонецЕсли;
	
	// Нет сохраненного ЭД и его необходимо создать
	Если НЕ ЕстьРабочийЭДД И ЯвляетсяКомандойФормированияЭД Тогда
		
		СообщенияОбОшибках = ПечатьДоговоровВызовСервера.СообщенияЗаполненияОбязательныхРеквизитов(
			ДоговорКонтрагента, ФорматированныйДокумент);
		
		Если СообщенияОбОшибках.Количество() = 0 Тогда
			
			Если Модифицированность Или Не ЗначениеЗаполнено(ФайлДоговора) Тогда
				СохранитьТекстДоговораВФайлДоговора();
			КонецЕсли;
			
		Иначе
			
			ПечатьДоговоровКлиент.ОткрытьФормуНавигацииПоОшибкамВыгрузки(СообщенияОбОшибках);
			Возврат;
			
		КонецЕсли;
		
	ИначеЕсли Модифицированность И ЕстьРабочийЭДД Тогда 
		
		// Запись текста договора для снятия признака модифицированности
		СохранитьТекстДоговораВФайлДоговора();
		ОбщегоНазначенияКлиент.СообщитьПользователю(
			НСтр("ru = 'Существует ранее сохраненный электронный документ, который не содержит внесенные изменения'"));
		
	КонецЕсли;			
	
	ЭлектронноеВзаимодействиеКлиент.ВыполнитьПодключаемуюКомандуЭДО(Команда, ЭтотОбъект, ДоговорОбъект);
			
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОбновитьКомандыЭДО() 
	
	Если ТипЗнч(ОбъектПечати) = Тип("СправочникСсылка.ДоговорыКонтрагентов") 
		 И ЭлектронныйФормат Тогда
		ОбменСКонтрагентамиКлиент.ОбновитьКоманды(ЭтотОбъект, ДоговорОбъект);
	КонецЕсли;   
	
КонецПроцедуры

&НаСервере
Процедура СоздатьОбъектДоговорКонтрагента()
	
	Договор = ДоговорКонтрагента.ПолучитьОбъект();
	ЗначениеВРеквизитФормы(Договор, "ДоговорОбъект");
	
КонецПроцедуры

// Конец ЭлектронноеВзаимодействие.ОбменСКонтрагентами

&НаСервере
Функция ИмяДоговора()
	
	Если ЭтоДополнительноеСоглашение Тогда
		ИмяДоговора = СокрЛП(СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Дополнительное соглашение %1 от %2 к договору № %3 от %4'"),
			НомерСоглашения,
			Формат(ДатаСоглашения, "ДЛФ=DD"),
			НомерДоговора,
			Формат(ДатаДоговора, "ДЛФ=DD")));
	Иначе
		ИмяДоговора = СокрЛП(СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Договор № %1 от %2'"), НомерДоговора, Формат(ДатаДоговора, "ДЛФ=DD")));
	КонецЕсли;
	
	Возврат ИмяДоговора;
	
КонецФункции

&НаСервереБезКонтекста
Функция ВидДокументаДополнительноеСоглашениеЭДО()
	
	Возврат ЭлектронныеДокументыЭДО.ВидДокументаПоТипу(Перечисления.ТипыДокументовЭДО.ДополнительноеСоглашение);
	
КонецФункции

&НаКлиенте
Процедура ОтправитьФайлСоглашенияПоЭДО(ДанныеФайла)
	
	ДвоичныеДанные = ПолучитьИзВременногоХранилища(ДанныеФайла.СсылкаНаДвоичныеДанныеФайла);
	
	УдалитьИзВременногоХранилища(ДанныеФайла.СсылкаНаДвоичныеДанныеФайла);
	
	ПараметрыФормы = Новый Структура;
	ПараметрыФормы.Вставить("ДвоичныеДанныеФайла", ДвоичныеДанные);
	ПараметрыФормы.Вставить("НаименованиеФайла", ДанныеФайла.Наименование);
	ПараметрыФормы.Вставить("Расширение", ДанныеФайла.Расширение);
	ПараметрыФормы.Вставить("Основание", ДоговорКонтрагента);
	ПараметрыФормы.Вставить("Уникальность", ДанныеФайла.Наименование);
	ПараметрыФормы.Вставить("ВидДокумента", ВидДокументаДополнительноеСоглашениеЭДО());
	ПараметрыФормы.Вставить("НомерДокумента", НомерСоглашения);
	ПараметрыФормы.Вставить("ДатаДокумента", ДатаСоглашения);
	ПараметрыФормы.Вставить("СопроводительнаяЗаписка", КомментарийСоглашения);
	
	ИнтерфейсДокументовЭДОКлиент.ОткрытьФормуЭлектронногоДокумента(ПараметрыФормы);
	
КонецПроцедуры

&НаКлиентеНаСервереБезКонтекста
Функция РеквизитыДополнительногоСоглашения(НомерСоглашения, ДатаСоглашения)
	
	РеквизитыДополнительногоСоглашения = ПечатьДоговоровКлиентСервер.РеквизитыДополнительногоСоглашения();
	РеквизитыДополнительногоСоглашения.НомерСоглашения = НомерСоглашения;
	РеквизитыДополнительногоСоглашения.ДатаСоглашения = ДатаСоглашения;
	
	Возврат РеквизитыДополнительногоСоглашения;
	
КонецФункции

#КонецОбласти

#Область МобильныйКлиент

&НаСервере
Процедура МобильныйКлиентАдаптацияФормы()
	
	Если Не ОбщегоНазначения.ЭтоМобильныйКлиент() Тогда
		Возврат;
	КонецЕсли;
	
	МобильныйКлиентБПФормаОбъекта.НастроитьФорму(ЭтотОбъект);
	АдаптироватьМККоманднаяПанельФормы();
	АдаптироватьМКШапкаФормы();
	
КонецПроцедуры

&НаСервере
Процедура АдаптироватьМККоманднаяПанельФормы()
	
	КомандыМК = "ФормаОтправить,ФормаСохранить";
	МобильныйКлиентБПФормаОбъекта.АдаптироватьМККоманднаяПанельФормаПечатиДокументов(ЭтотОбъект, КомандыМК);
	
	Элементы.ФормаСохранить.Заголовок = НСтр("ru = 'Отправить в ...'");
	Элементы.ФормаСохранить.Картинка = БиблиотекаКартинок.ПередачаДанныхНаУстройствеМК;
	Элементы.ФормаОтправить.Заголовок = НСтр("ru = 'Отправить по почте'");
	МобильныйКлиентБПФормаОбъекта.ПеренестиКомандуВМККомандыФормы(Элементы, Элементы.ФормаПечать);
	
КонецПроцедуры

&НаСервере
Процедура АдаптироватьМКШапкаФормы()
	
	Элементы.ДекорацияУведомление.Заголовок = НСтр("ru = 'Документ содержит метки электронного формата. Они выделены серым цветом. Электронные метки не выводятся на печать.'");
	МобильныйКлиентБПФормаОбъекта.ВстроитьЭлементФормыВГруппаМКФормаВажное(Элементы, Элементы.ГруппаБаннерПодсказка);
	МобильныйКлиентБПФормаОбъекта.ВстроитьЭлементФормыВГруппаМКФормаВажное(Элементы, Элементы.ФорматированныйДокумент);
	МобильныйКлиентБПФормаОбъекта.ВстроитьЭлементФормыВГруппаМКФормаВажное(Элементы, Элементы.ГруппаСостояниеЭДО);
	
	МобильныйКлиентБПФормаОбъекта.СкрытьЭлементФормы(Элементы, Элементы.ГруппаКоманднаяПанель);
	
КонецПроцедуры

#КонецОбласти
