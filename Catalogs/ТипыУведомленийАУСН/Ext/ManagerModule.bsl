#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// Возвращает реквизиты справочника, которые образуют естественный ключ
//  для элементов справочника.
//
// Возвращаемое значение:
//  Массив(Строка) - массив имен реквизитов, образующих естественный ключ.
//
Функция ПоляЕстественногоКлюча() Экспорт
	
	Результат = Новый Массив();
	
	Результат.Добавить("Код");
	
	Возврат Результат;
	
КонецФункции

// Возвращает тип уведомления, используемый по умолчанию, например,
// в тех случаях, когда сервис АУСН загрузил уведомление неизвестного типа
//
// Возвращаемое значение:
//  СправочникСсылка.ТипыУведомленийАУСН
//
Функция ТипУведомленияПоУмолчанию() Экспорт
	
	Возврат ИноеОповещение;
	
КонецФункции

#КонецОбласти

#КонецЕсли