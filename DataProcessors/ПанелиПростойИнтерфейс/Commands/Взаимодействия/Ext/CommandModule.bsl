#Область ОбработчикиСобытий

&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
		
	ПараметрыОткрытия = ОбщегоНазначенияБПКлиентСервер.ПараметрыОткрытияФормыСОжиданием(ПараметрыВыполненияКоманды);
	ЗаголовокФормы         = НСтр("ru = 'История взаимодействий (звонки, письма, встречи)'");
	ПараметрыОткрытия.Заголовок               = ЗаголовокФормы;
	ПараметрыОткрытия.ИмяФормы                = "ЖурналДокументов.Взаимодействия.Форма.ФормаСпискаБП";
	ПараметрыОткрытия.ЗамерПроизводительности = "СозданиеФормыЖурналаВзаимодействие";
	
	ПараметрыФормы = Новый Структура;
	ПараметрыФормы.Вставить("Заголовок", ЗаголовокФормы);
	
	// Не связываем форму списка с навигационной ссылкой команды, 
	// чтобы не смущать пользователя словами про оценку производительности в тексте ссылки, 
	// который могут пересылать другим пользователям.
	ПараметрыОткрытия.НавигационнаяСсылка     = Неопределено;
	
	ОбщегоНазначенияБПКлиент.ОткрытьФормуСОжиданием(ПараметрыОткрытия, ПараметрыФормы);
	
КонецПроцедуры

#КонецОбласти