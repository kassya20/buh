#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий

Процедура ОбработкаПолученияДанныхВыбора(ДанныеВыбора, Параметры, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	
	Период = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(Параметры, "Период", Неопределено);
	Если Не ЗначениеЗаполнено(Период) Тогда
		Период = ОбщегоНазначения.ТекущаяДатаПользователя();
	КонецЕсли;
	
	ДанныеВыбора = Новый СписокЗначений;
	// п.1.2 ст.430 НК РФ
	ДанныеВыбора.Добавить(Единый);
	
	Если УчетСтраховыхВзносовИПКлиентСервер.ДоступенТарифДляВоенныхПенсионеров(Период) Тогда
		// п.1.4 ст.430 НК РФ
		ДанныеВыбора.Добавить(ВоенныйПенсионер);
	КонецЕсли;
	
	Если УчетСтраховыхВзносовИПКлиентСервер.ПрименяетсяТарифУчастникСЭЗ(Период)
		И ИнтеграцияСБанкамиПовтИсп.ИнтеграцияВИнформационнойБазеВключена() Тогда
		// п.1.3 ст.430 НК РФ
		ДанныеВыбора.Добавить(УчастникСЭЗнаНТ);
		// п.1.4 ст.430 НК РФ
		ДанныеВыбора.Добавить(ВоенныйПенсионерСЭЗ);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#КонецЕсли