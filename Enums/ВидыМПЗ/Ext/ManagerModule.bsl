#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда
	
#Область СлужебныйПрограммныйИнтерфейс

// Возвращает вид МПЗ, который подходит для списания
// 
// Параметры:
//  ВидМПЗУчета - ПеречислениеСсылка.ВидыМПЗ - вид МПЗ учета
//  ТипСписания - Строка - "Реализация", "Списание", "Перемещение" - тип списания.
//                         "Перемещение" указывается при перемещении в НТТ
// 
// Возвращаемое значение:
//  ПеречислениеСсылка.ВидыМПЗ - Вид МПЗ при списании
Функция ЗначениеПриСписании(ВидМПЗУчета, ТипСписания) Экспорт
	
	Если НРег(ТипСписания) = НРег("Списание") Тогда
		Возврат ВидМПЗУчета;
	КонецЕсли;
	
	Если ВидМПЗУчета = Материалы Или ВидМПЗУчета = ВспомогательныеМатериалы Тогда
		Возврат Товары;
	ИначеЕсли ВидМПЗУчета = Полуфабрикаты Тогда
		Возврат Продукция;
	Иначе
		Возврат ВидМПЗУчета
	КонецЕсли;
	
КонецФункции

// Возвращает значения, для которых допустимо менять счет учета
// 
// Возвращаемое значение:
//  Массив из ПеречислениеСсылка.ВидыМПЗ - перемещаемые значения
Функция ПеремещаемыеЗначения() Экспорт
	
	СписокЗначений = Новый Массив;
	СписокЗначений.Добавить(Товары);
	СписокЗначений.Добавить(Материалы);
	СписокЗначений.Добавить(ВспомогательныеМатериалы);
	СписокЗначений.Добавить(Полуфабрикаты);
	СписокЗначений.Добавить(Продукция);
	
	Возврат СписокЗначений;
	
КонецФункции

#КонецОбласти

#КонецЕсли