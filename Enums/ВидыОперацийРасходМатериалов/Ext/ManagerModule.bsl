#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда
	
Процедура ОбработкаПолученияДанныхВыбора(ДанныеВыбора, Параметры, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	ДанныеВыбора = Новый СписокЗначений;
	ДанныеВыбора.Добавить(ИспользованиеМатериалов, "Использование материалов");
	ДанныеВыбора.Добавить(ПередачаСотруднику, "Передача сотруднику");
	
	Если ПолучитьФункциональнуюОпцию("ИспользоватьВозвратнуюТару")
		И Параметры.Свойство("Организация") 
		И ЗначениеЗаполнено(Параметры.Организация)
		И Параметры.Свойство("Дата") 
		И ЗначениеЗаполнено(Параметры.Дата) Тогда
		
		Если УчетнаяПолитика.УчитыватьТаруЗаБалансом(Параметры.Организация, Параметры.Дата) Тогда
			ДанныеВыбора.Добавить(ВозвратнаяТара, "Передача тары в эксплуатацию");
		КонецЕсли;		
	КонецЕсли;
	
КонецПроцедуры

#КонецЕсли