#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// Возвращает текстовое представление вида расхода УСН
//
// Параметры:
//  ВидРасхода - ПеречислениеСсылка.ВидыРасходовУСН - вид расхода для преобразования
// 
// Возвращаемое значение:
//  Строка - понятное значение вида расхода УСН
//
Функция ПредставлениеВидаРасходаУСН(ВидРасхода) Экспорт
	
	Если ВидРасхода = Перечисления.ВидыРасходовУСН.Номенклатура Тогда
		Результат = НСтр("ru = 'Товары и материалы'");
	ИначеЕсли ВидРасхода = Перечисления.ВидыРасходовУСН.ДопРасходы Тогда
		Результат = НСтр("ru = 'Дополнительные расходы'");
	ИначеЕсли ВидРасхода = Перечисления.ВидыРасходовУСН.Зарплата Тогда
		Результат = НСтр("ru = 'Зарплата'");
	ИначеЕсли ВидРасхода = Перечисления.ВидыРасходовУСН.Командировки Тогда
		Результат = НСтр("ru = 'Командировки'");
	ИначеЕсли ВидРасхода = Перечисления.ВидыРасходовУСН.ЛизинговыеПлатежи Тогда
		Результат = НСтр("ru = 'Арендные (лизинговые) платежи'");
	ИначеЕсли ВидРасхода = Перечисления.ВидыРасходовУСН.Налоги Тогда
		Результат = НСтр("ru = 'Налоги и взносы'");
	ИначеЕсли ВидРасхода = Перечисления.ВидыРасходовУСН.НДС Тогда
		Результат = НСтр("ru = 'НДС'");
	ИначеЕсли ВидРасхода = Перечисления.ВидыРасходовУСН.НДС_ТС Тогда
		Результат = НСтр("ru = 'НДС таможенного союза'");
	ИначеЕсли ВидРасхода = Перечисления.ВидыРасходовУСН.НМА Тогда
		Результат = НСтр("ru = 'Нематериальные активы'");
	ИначеЕсли ВидРасхода = Перечисления.ВидыРасходовУСН.ОС Тогда
		Результат = НСтр("ru = 'Основные средства'");
	ИначеЕсли ВидРасхода = Перечисления.ВидыРасходовУСН.РБП Тогда
		Результат = НСтр("ru = 'Расходы будущих периодов'");
	ИначеЕсли ВидРасхода = Перечисления.ВидыРасходовУСН.ТаможенныеПлатежи Тогда
		Результат = НСтр("ru = 'Таможенные платежи'");
	ИначеЕсли ВидРасхода = Перечисления.ВидыРасходовУСН.Услуги Тогда
		Результат = НСтр("ru = 'Услуги'");
	Иначе
		Результат = Строка(ВидРасхода);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти

#КонецЕсли
