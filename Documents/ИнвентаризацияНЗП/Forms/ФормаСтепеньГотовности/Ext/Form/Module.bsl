#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	СтепеньГотовности = Параметры.СтепеньГотовности;
	СтепеньГотовностиУказанаВШапке = Параметры.СтепеньГотовностиУказанаВШапке;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ОК(Команда)
	
	РезультатФормы = Неопределено;
	
	Если Модифицированность Тогда
		РезультатФормы = Новый Структура();
		РезультатФормы.Вставить("СтепеньГотовности",     СтепеньГотовности);
		РезультатФормы.Вставить("СтепеньГотовностиУказанаВШапке", СтепеньГотовностиУказанаВШапке = 1);
	КонецЕсли;
	
	Закрыть(РезультатФормы);
	
КонецПроцедуры

#КонецОбласти
