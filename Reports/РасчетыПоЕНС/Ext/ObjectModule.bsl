#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий

Процедура ОбработкаПроверкиЗаполнения(Отказ, ПроверяемыеРеквизиты)
	
	Проверки = Новый Структура("КорректностьПериода", Истина);
	БухгалтерскиеОтчетыВызовСервера.ОбработкаПроверкиЗаполнения(ЭтотОбъект, Отказ, Проверки);
	
	НачалоПростогоУчетаЕНС = ЕдиныйНалоговыйСчет.НачалоПростогоУчета();
	Если НачалоПериода < НачалоПростогоУчетаЕНС
		И КонецПериода >= НачалоПростогоУчетаЕНС Тогда
		ТекстСообщения = СтрШаблон(
			НСтр("ru='Отчеты за период после %1 и до этой даты формируются отдельно. Измените период отчета.'"),
		Формат(НачалоПростогоУчетаЕНС, "ДЛФ=D"));
		ОбщегоНазначения.СообщитьПользователю(ТекстСообщения, , "Отчет.НачалоПериода", , Отказ);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#КонецЕсли