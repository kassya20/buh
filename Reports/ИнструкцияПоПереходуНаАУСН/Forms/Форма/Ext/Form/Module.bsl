#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("АвтоТест") Тогда
		Возврат;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Параметры.Организация) Тогда
		ЭтоЮрЛицо = Справочники.Организации.ЭтоЮрЛицо(Параметры.Организация);
	Иначе
		ЭтоЮрЛицо = Параметры.ЭтоЮрЛицо;
	КонецЕсли;

	СсылкаНаЛичныйКабинетНалогоплательщика = СсылкаНаЛичныйКабинетНалогоплательщика(ЭтоЮрЛицо);

	Элементы.СпособыОтправкиУведомления.Заголовок = СтроковыеФункции.ФорматированнаяСтрока(СтрШаблон(
		НСтр("ru = ' Уведомление можно направить через:
		|    • <a href=""%1"">Личный кабинет налогоплательщика</a> на сайте ФНС
		|    • Обслуживающий банк'"),
		СсылкаНаЛичныйКабинетНалогоплательщика));

КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Возвращает ссылку на личный кабинет налогоплательщика.
//
// Параметры:
//   ЭтоЮрЛицо - Булево
//
// Возвращаемое значение:
//   Строка
//
Функция СсылкаНаЛичныйКабинетНалогоплательщика(ЭтоЮрЛицо) Экспорт
	
	Если ЭтоЮрЛицо Тогда
		Возврат "http://lkul.nalog.ru/";
	Иначе
		Возврат "https://lkip2.nalog.ru/";
	КонецЕсли;
	
КонецФункции

#КонецОбласти